USER INTENT / PROMPT:
Kannst du mir den Aufbau des Meta-Orchestrators erklären?

==================================================
AUTOMATISCH EINGEBETTETER KONTEXT (ML-gesteuert):
==================================================
--- Datei: /home/joe/Dokumente/projects/Apps/partner/idee.txt (Relevanz: 0.48) ---
# 🏗️ META-CLAWD ORCHESTRATOR - KOMPLETTER BAUPLAN **Version:** 1.0 **Datum:** 2025-01-15 **Geschätzter Aufwand:** 2-4 Stunden Setup, dann sofort nutzbar **Kosten:** $0 (alles lokal) --- ## 📋 INHALTSVERZEICHNIS 1. [Übersicht & Architektur](#1-übersicht--architektur) 2. [Voraussetzungen](#2-voraussetzungen) 3. [Komponenten-Details](#3-komponenten-details) 4. [Installation Schritt-für-Schritt](#4-installation-schritt-für-schritt) 5. [Konfiguration](#5-konfiguration) 6. [Erste Nutzung](#6-erste-nutzung) 7. [Erweiterte Features](#7-erweiterte-features) 8. [Troubleshooting](#8-troubleshooting) 9. [Maintenance & Updates](#9-maintenance--updates) --- ## 1. ÜBERSICHT & ARCHITEKTUR ### 1.1 Was wird gebaut? ``` ┌────────────────────────────────────────────────────────────────┐ │ META-CLAWD SYSTEM │ ├────────────────────────────────────────────────────────────────┤ │ │ │ ┌──────────────────────────────────────────────────────────┐ │ │ │ Layer 1: Meta-Orchestrator (Nano-Clawd Session) │ │ │ │ - Hauptsteuerung │ │ │ │ - Natürliche Sprach-Interface │ │ │ │ - Tool-Orchestrierung │ │ │ └──────────────────┬───────────────────────────────────────┘ │ │ │ │ │ ┌──────────────────┴───────────────────────────────────────┐ │ │ │ Layer 2: Core Tools │ │ │ │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │ │ │ │ │ RAG Engine │ │ Session │ │ Context │ │ │ │ │ │ │ │ Manager │ │ Gatherer │ │ │ │ │ │ - Index │ │ - Branching │ │ - Git │ │ │ │ │ │ - Search │ │ - Compare │ │ - Files │ │ │ │ │ │ - Embed │ │ - Merge │ │ - System │ │ │ │ │ └─────────────┘ └─────────────┘ └─────────────┘ │ │ │ └──────────────────────────────────────────────────────────┘ │ │ │ │ ┌──────────────────────────────────────────────────────────┐ │ │ │ Layer 3: Storage & State │ │ │ │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │ │ │ │ │ SQLite DB │ │ FAISS Index │ │ Sessions/ │ │ │ │ │ │ (Metadata) │ │ (Vectors) │ │ Logs │ │ │ │ │ └─────────────┘ └─────────────┘ └─────────────┘ │ │ │ └──────────────────────────────────────────────────────────┘ │ │ │ │ ┌──────────────────────────────────────────────────────────┐ │ │ │ Layer 4: Agent Pool (dynamisch) │ │ │ │ [Coding] [Research] [Debug] [Optimize] [...] │ │ │ └──────────────────────────────────────────────────────────┘ │ └────────────────────────────────────────────────────────────────┘ ``` ### 1.2 Datenfluss ``` User Input │ ▼ Meta-Clawd empfängt │ ├─► Context Gatherer holt System-Kontext │ │ │ ├─► Git Status │ ├─► Offene Dateien │ ├─► Recent Commands │ └─► Environment Vars │ ├─► RAG Engine sucht relevanten Kontext │ │ │ ├─► Semantic Search in Index │ ├─► Findet ähnliche Probleme │ └─► Holt passende Code-Beispiele │ ├─► Meta-Clawd analysiert & entscheidet │ │ │ ├─► Selbst lösen? → Direct Action │ │ │ ├─► Unsicher? → Branching │ │ │ │ │ ├─► Session A: Approach X │ │ ├─► Session B: Approach Y │ │ └─► Compare & Merge Best │ │ │ └─► Komplex? → Delegation │ │ │ ├─► Start Specialized Agent │ ├─► Monitor Progress │ └─► Collect Results │ └─► Output zurück an User │ └─► Index Update (lernt aus Session) ``` ### 1.3 Dateisystem-Struktur ``` ~/ai-workspace/ ├── meta-orchestrator/ # Haupt-Meta-Session │ ├── system_prompt.md # Instruktionen für Meta-Clawd │ ├── tools_config.yaml # Tool-Definitionen │ ├── knowledge.db # SQLite Metadata │ ├── faiss.index # Vector Index │ ├── faiss.index.metadata # Index Metadata │ ├── session_history.jsonl # Conversation Logs │ └── tools/ # Tool Scripts │ ├── rag_engine.py │ ├── session_manager.py │ ├── context_gatherer.py │ └── agent_spawner.py │ ├── sessions/ # Branch-Sessions │ ├── experiment-numpy_a3f9c2/ │ ├── experiment-cython_7d2e8f/ │ └── debug-session_4b6a1c/ │ ├── agents/ # Specialized Agents │ ├── coding-agent/ │ ├── research-agent/ │ └── debug-agent/ │ └── knowledge-base/ # Raw Indexed Content ├── conversations/ ├── code-snippets/ ├── documentation/ └── outputs/ ``` --- ## 2. VORAUSSETZUNGEN ### 2.1 Hardware **Minimum:** - CPU: 4 Cores - RAM: 8GB - Disk: 10GB frei - GPU: Keine nötig (CPU-Inference ausreichend) **Empfohlen:** - CPU: 8+ Cores - RAM: 16GB+ - Disk: 50GB+ SSD - GPU: RX 6800 XT / RTX 3060+ (optional, für schnelleres Embedding) ### 2.2 Software **Basis:** ```bash # OS: Linux (Ubuntu 22.04+, Arch, Fedora) oder macOS # Shell: bash/zsh # Python: 3.10+ # Git: 2.30+ # tmux: 3.0+ (für Session-Management) ``` **Python-Packages:** ```txt sentence-transformers>=2.2.0 faiss-cpu>=1.7.4 # oder faiss-gpu wenn CUDA/ROCm transformers>=4.30.0 torch>=2.0.0 flask>=2.3.0 pyyaml>=6.0 sqlite3 # (built-in) numpy>=1.24.0 tqdm>=4.65.0 ``` **Optional:** ```txt llama-cpp-python>=0.2.0 # Für lokale Modelle (Qwen, Mistral) chromadb>=0.4.0 # Alternative zu FAISS langchain>=0.1.0 # Wenn du LangChain nutzen willst ``` ### 2.3 Existing Setup **Du brauchst bereits:** - Nano-Clawd / Claude Desktop / Vergleichbares AI-System installiert - API-Zugang (Claude, OpenAI) ODER lokales Modell (Ollama, LM Studio) **Check:** ```bash # Teste ob Clawd funktioniert clawd --version # Oder API-Test curl https://api.anthropic.com/v1/messages \ -H "x-api-key: $ANTHROPIC_API_KEY" \ -H "content-type: application/json" \ -d '{"model":"claude-3-5-sonnet-20241022","messages":[{"role":"user","content":"test"}],"max_tokens":10}' ``` --- ## 3. KOMPONENTEN-DETAILS ### 3.1 RAG Engine **Datei:** `tools/rag_engine.py` **Funktionen:** - Semantic Search über alle indexierten Inhalte - Embedding-Generierung (sentence-transformers) - Vector Storage (FAISS) - Metadata Management (SQLite) **Performance:** - Embedding: ~500 docs/sec (CPU) - Search: <50ms für 10K docs - Speicher: ~1MB per 1000 docs **Code:** (siehe unten in Installationsabschnitt) ### 3.2 Session Manager **Datei:** `tools/session_manager.py` **Funktionen:** - Branch-Erstellung (parallele Experimente) - Session-Monitoring (via tmux) - Output-Aggregation - Result-Comparison **Workflow:** ``` create_branch() → tmux new-session → clawd --prompt "{task}" → monitor output → store in session_dir/ compare_branches() → read all outputs → analyze differences → rank by criteria merge_best() → user chooses (or auto-select) → copy to main workspace → update RAG index ``` ### 3.3 Context Gatherer **Datei:** `tools/context_gatherer.py` **Sammelt:** - Git: Status, branch, recent commits, diff - Files: Offene Dateien (via lsof), recent edits - Shell: Command history, environment vars - System: Load, memory, disk usage - Time: Current time, timezone, locale **Output-Format:** ```json { "timestamp": "2025-01-15T14:30:00Z", "git": { "repo": "my-project", "branch": "feature/optimization", "status": "M utils.py\nA new_feature.py", "recent_commits": [...] }, "files": { "open": ["/home/user/project/utils.py", ...], "recent": ["/home/user/project/tests.py", ...] }, "shell": { "last_commands": ["git status", "pytest", ...], "cwd": "/home/user/project" }, "system": { "load": [0.5, 0.6, 0.7], "memory_free": "8GB", "disk_free": "120GB" } } ``` ### 3.4 Agent Spawner **Datei:** `tools/agent_spawner.py` **Agent-Typen:** - **Coding:** Code-Generierung, Refactoring - **Research:** Information-Gathering, Documentation - **Debug:** Error-Analysis, Fix-Suggestions - **Optimize:** Performance-Tuning, Profiling - **Test:** Test-Generierung, Coverage-Analysis **Spawning:** ```python def spawn_agent(agent_type, task, context): agent_dir = workspace / "agents" / f"{agent_type}-{uuid}" # Agent-spezifisches System-Prompt system_prompt = load_agent_prompt(agent_type) # Starte in tmux tmux_session = f"agent-{agent_type}-{uuid}" subprocess.run([ 'tmux', 'new-session', '-d', '-s', tmux_session, 'clawd', '--system-prompt', system_prompt, '--initial-message', task, '--context', json.dumps(context), '--output-file', agent_dir / 'output.log' ]) return tmux_session ``` --- ## 4. INSTALLATION SCHRITT-FÜR-SCHRITT ### 4.1 Workspace Erstellen ```bash # Erstelle Basis-Struktur mkdir -p ~/ai-workspace/{meta-orchestrator/tools,sessions,agents,knowledge-base/{conversations,code-snippets,documentation,outputs}} cd ~/ai-workspace/meta-orchestrator ``` ### 4.2 Python Environment ```bash # Virtual Environment python3 -m venv venv source venv/bin/activate # Dependencies cat > requirements.txt << 'EOF' sentence-transformers>=2.2.0 faiss-cpu>=1.7.4 transformers>=4.30.0 torch>=2.0.0 flask>=2.3.0 pyyaml>=6.0 numpy>=1.24.0 tqdm>=4.65.0 watchdog>=3.0.0 EOF pip install -r requirements.txt # Test python -c "from sentence_transformers import SentenceTransformer; print('✓ OK')" ``` ### 4.3 RAG Engine Installation ```bash cd ~/ai-workspace/meta-orchestrator/tools cat > rag_engine.py << 'EOFPYTHON' #!/usr/bin/env python3 """ RAG Engine - Semantic Search & Knowledge Management """ import json import sqlite3 from pathlib import Path from datetime import datetime import sys try: from sentence_transformers import SentenceTransformer import faiss import numpy as np except ImportError: print("Error: Missing dependencies. Run: pip install sentence-transformers faiss-cpu numpy") sys.exit(1) class RAGEngine: """Retrieval-Augmented Generation Engine""" def __init__(self, workspace_dir=None): if workspace_dir is None: workspace_dir = Path.home() / "ai-workspace" / "meta-orchestrator" self.workspace = Path(workspace_dir) self.db_path = self.workspace / "knowledge.db" self.index_path = self.workspace / "faiss.index" self.metadata_path = self.workspace / "faiss.index.metadata" print(f"Initializing RAG Engine at {self.workspace}") # Load embedding model (lightweight, ~80MB) print("Loading embedding model (all-MiniLM-L6-v2)...") self.model = SentenceTransformer('all-MiniLM-L6-v2') self.embedding_dim = 384 # Initialize storage self.init_db() self.init_index() print("✓ RAG Engine ready") def init_db(self): """Initialize SQLite database for metadata""" self.db = sqlite3.connect(str(self.db_path)) self.db.execute(""" CREATE TABLE IF NOT EXISTS documents ( id INTEGER PRIMARY KEY AUTOINCREMENT, type TEXT NOT NULL, content TEXT NOT NULL, metadata TEXT, embedding_id INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ) """) self.db.execute(""" CREATE INDEX IF NOT EXISTS idx_type ON documents(type) """) self.db.execute(""" CREATE INDEX IF NOT EXISTS idx_created ON documents(created_at) """) self.db.commit() def init_index(self): """Initialize or load FAISS vector index""" if self.index_path.exists(): print(f"Loading existing index from {self.index_path}") self.index = faiss.read_index(str(self.index_path)) # Load metadata if self.metadata_path.exists(): with open(self.metadata_path, 'r') as f: self.index_metadata = json.load(f) else: self.index_metadata = {'total': self.index.ntotal} else: print("Creating new FAISS index") self.index = faiss.IndexFlatL2(self.embedding_dim) self.index_metadata = {'total': 0} def index_document(self, content, doc_type, metadata=None): """Add document to index""" if not content or not content.strip(): return None # Generate embedding embedding = self.model.encode([content])[0] # Add to FAISS embedding_id = self.index.ntotal self.index.add(np.array([embedding], dtype=np.float32)) # Save to database cursor = self.db.execute(""" INSERT INTO documents (type, content, metadata, embedding_id) VALUES (?, ?, ?, ?) """, ( doc_type, content, json.dumps(metadata or {}), embedding_id )) doc_id = cursor.lastrowid self.db.commit() # Update metadata self.index_metadata['total'] = self.index.ntotal # Save index periodically (every 10 docs) if self.index.ntotal % 10 == 0: self.save_index() return doc_id def save_index(self): """Persist index to disk""" faiss.write_index(self.index, str(self.index_path)) with open(self.metadata_path, 'w') as f: json.dump(self.index_metadata, f) def search(self, query, k=5, doc_type=None, min_similarity=0.0): """Semantic search""" if self.index.ntotal == 0: return [] # Generate query embedding query_embedding = self.model.encode([query])[0] # Search in FAISS distances, indices = self.index.search( np.array([query_embedding], dtype=np.float32), min(k * 2, self.index.ntotal) # Get more, then filter ) # Retrieve documents results = [] for idx, dist in zip(indices[0], distances[0]): if idx == -1: # FAISS returns -1 for empty slots continue cursor = self.db.execute(""" SELECT id, type, content, metadata, created_at FROM documents WHERE embedding_id = ? """, (int(idx),)) row = cursor.fetchone() if row: similarity = float(1 / (1 + dist)) # Convert L2 distance to similarity if similarity < min_similarity: continue doc = { 'id': row[0], 'type': row[1], 'content': row[2], 'metadata': json.loads(row[3]) if row[3] else {}, 'created_at': row[4], 'similarity': similarity } # Filter by type if specified if doc_type is None or doc['type'] == doc_type: results.append(doc) if len(results) >= k: break return results def index_conversation(self, messages, session_name, metadata=None): """Index a complete conversation""" conversation_text = "\n\n".join([ f"{msg.get('role', 'unknown')}: {msg.get('content', '')}" for msg in messages ]) meta = metadata or {} meta.update({ 'session': session_name, 'num_messages': len(messages), 'type': 'conversation' }) return self.index_document( content=conversation_text, doc_type='conversation', metadata=meta ) def index_code_file(self, file_path): """Index a single code file""" path = Path(file_path) if not path.exists(): print(f"⚠ File not found: {file_path}") return None try: content = path.read_text(encoding='utf-8') return self.index_document( content=content, doc_type='code', metadata={ 'file_path': str(path.absolute()), 'file_name': path.name, 'language': path.suffix[1:] if path.suffix else 'unknown', 'size': len(content) } ) except Exception as e: print(f"⚠ Error indexing {file_path}: {e}") return None def index_directory(self, directory, extensions=None, recursive=True): """Index all code files in a directory""" if extensions is None: extensions = {'.py', '.js', '.ts', '.jsx', '.tsx', '.rs', '.go', '.java', '.c', '.cpp', '.h', '.hpp', '.md', '.txt'} dir_path = Path(directory) indexed = 0 skipped = 0 pattern = '**/*' if recursive else '*' for file_path in dir_path.glob(pattern): if file_path.is_file() and file_path.suffix in extensions: # Skip common non-code files if any(part.startswith('.') for part in file_path.parts): skipped += 1 continue if self.index_code_file(file_path): indexed += 1 if indexed % 10 == 0: print(f" Indexed {indexed} files...") else: skipped += 1 self.save_index() print(f"✓ Indexed {indexed} files, skipped {skipped}") return indexed def stats(self): """Get index statistics""" cursor = self.db.execute(""" SELECT type, COUNT(*) as count FROM documents GROUP BY type """) stats = { 'total_documents': self.index.ntotal, 'by_type': {row[0]: row[1] for row in cursor.fetchall()} } return stats def main(): """CLI Interface""" if len(sys.argv) < 2: print(""" Usage: rag_engine.py search <query> [--type TYPE] [--limit N] rag_engine.py index <path> [--type TYPE] rag_engine.py index-dir <directory> [--recursive] rag_engine.py stats Examples: rag_engine.py search "how to optimize performance" rag_engine.py search "bug in API" --type code rag_engine.py index my_file.py rag_engine.py index-dir ./src --recursive rag_engine.py stats """) return rag = RAGEngine() command = sys.argv[1] if command == 'search': query = ' '.join(arg for arg in sys.argv[2:] if not arg.startswith('--')) # Parse options doc_type = None limit = 5 for i, arg in enumerate(sys.argv): if arg == '--type' and i + 1 < len(sys.argv): doc_type = sys.argv[i + 1] elif arg == '--limit' and i + 1 < len(sys.argv): limit = int(sys.argv[i + 1]) results = rag.search(query, k=limit, doc_type=doc_type) if not results: print("No results found.") return print(f"\nTop {len(results)} results for: {query}\n") for i, result in enumerate(results, 1): print(f"{i}. [{result['type']}] (similarity: {result['similarity']:.2%})") # Show metadata if result['metadata']: for key, value in result['metadata'].items(): print(f" {key}: {value}") # Show content preview content_preview = result['content'][:200].replace('\n', ' ') print(f" {content_preview}...") print() elif command == 'index': if len(sys.argv) < 3: print("Error: Missing file path") return file_path = sys.argv[2] doc_id = rag.index_code_file(file_path) if doc_id: print(f"✓ Indexed as document #{doc_id}") rag.save_index() elif command == 'index-dir': if len(sys.argv) < 3: print("Error: Missing directory path") return directory = sys.argv[2] recursive = '--recursive' in sys.argv rag.index_directory(directory, recursive=recursive) elif command == 'stats': stats = rag.stats() print("\n📊 RAG Index Statistics\n") print(f"Total documents: {stats['total_documents']}") print("\nBy type:") for doc_type, count in stats['by_type'].items(): print(f" {doc_type}: {count}") print() else: print(f"Unknown command: {command}") if __name__ == '__main__': main() EOFPYTHON chmod +x rag_engine.py # Test python rag_engine.py stats ``` ### 4.4 Context Gatherer Installation ```bash cat > context_gatherer.py << 'EOFPYTHON' #!/usr/bin/env python3 """ Context Gatherer - Collects system context for Meta-Clawd """ import json import subprocess import os from pathlib import Path from datetime import datetime def run_command(cmd, timeout=5): """Run shell command safely""" try: result = subprocess.run( cmd, shell=True, capture_output=True, text=True, timeout=timeout ) return result.stdout.strip() except: return None def get_git_context(): """Git repository context""" # Check if in git repo if run_command("git rev-parse --git-dir 2>/dev/null") is None: return None return { 'repo': Path.cwd().name, 'branch': run_command("git branch --show-current"), 'status': run_command("git status --short"), 'recent_commits': run_command("git log --oneline -5"), 'diff_stats': run_command("git diff --stat"), 'uncommitted_changes': run_command("git diff --name-only") or "" } def get_file_context(): """Recently edited files""" # Files modified in last hour recent = run_command( "find . -type f -mmin -60 -name '*.py' -o -name '*.js' -o -name '*.md' 2>/dev/null | head -10" ) # Open files (via lsof) open_files = run_command( f"lsof -u {os.getenv('USER')} 2>/dev/null | grep -E '\\.(py|js|md|txt)$' | awk '{{print $NF}}' | head -10" ) return { 'cwd': str(Path.cwd()), 'recent_edits': recent.split('\n') if recent else [], 'open_files': open_files.split('\n') if open_files else [] } def get_shell_context(): """Shell history and environment""" history_file = Path.home() / ".bash_history" last_commands = [] if history_file.exists(): try: with open(history_file, 'r') as f: last_commands = f.readlines()[-20:] last_commands = [cmd.strip() for cmd in last_commands] except: pass return { 'last_commands': last_commands, 'shell': os.getenv('SHELL', '/bin/bash'), 'user': os.getenv('USER'), 'home': str(Path.home()) } def get_system_context(): """System resources""" load = run_command("uptime | awk -F'load average:' '{print $2}'") mem = run_command("free -h | awk 'NR==2{print $7}'") disk = run_command("df -h . | awk 'NR==2{print $4}'") return { 'load_average': load, 'memory_available': mem, 'disk_available': disk, 'hostname': run_command("hostname") } def gather_all(): """Gather complete context""" context = { 'timestamp': datetime.now().isoformat(), 'git': get_git_context(), 'files': get_file_context(), 'shell': get_shell_context(), 'system': get_system_context() } return context def main(): """CLI Interface""" context = gather_all() # Pretty print print(json.dumps(context, indent=2)) if __name__ == '__main__': main() EOFPYTHON chmod +x context_gatherer.py # Test python context_gatherer.py ``` ### 4.5 Session Manager Installation ```bash cat > session_manager.py << 'EOFPYTHON' #!/usr/bin/env python3 """ Session Manager - Handles branch sessions and comparison """ import json import subprocess import hashlib from pathlib import Path from datetime import datetime import sys import time class SessionManager: """Manages Clawd branch sessions""" def __init__(self, workspace=None): if workspace is None: workspace = Path.home() / "ai-workspace" / "sessions" self.workspace = Path(workspace) self.workspace.mkdir(parents=True, exist_ok=True) def create_branch(self, name, prompt, parent_session=None): """Create new branch session""" # Generate unique session ID session_id = hashlib.md5( f"{name}{datetime.now()}".encode() ).hexdigest()[:8] session_dir = self.workspace / f"{name}_{session_id}" session_dir.mkdir(exist_ok=True) # Metadata metadata = { 'id': session_id, 'name': name, 'created_at': datetime.now().isoformat(), 'parent': parent_session, 'status': 'created', 'prompt': prompt } (session_dir / 'metadata.json').write_text( json.dumps(metadata, indent=2) ) # Prompt file (session_dir / 'prompt.txt').write_text(prompt) print(f"✓ Created session: {session_id} ({name})") print(f" Directory: {session_dir}") return session_id, session_dir def start_session(self, session_id, command="clawd"): """Start session in tmux""" # Find session directory session_dir = self.find_session_dir(session_id) if not session_dir: print(f"✗ Session not found: {session_id}") return False # Read metadata metadata = json.loads((session_dir / 'metadata.json').read_text()) prompt = (session_dir / 'prompt.txt').read_text() # Tmux session name tmux_name = f"session-{session_id}" # Create tmux session subprocess.run([ 'tmux', 'new-session', '-d', '-s', tmux_name, '-c', str(session_dir) ]) # Send command subprocess.run([ 'tmux', 'send-keys', '-t', tmux_name, f'echo "Session: {metadata["name"]}"', 'Enter' ]) subprocess.run([ 'tmux', 'send-keys', '-t', tmux_name, f'echo "{prompt}" | tee initial_prompt.txt', 'Enter' ]) # Start logging log_file = session_dir / 'session.log' subprocess.run([ 'tmux', 'pipe-pane', '-t', tmux_name, f'cat >> {log_file}' ]) # Update metadata metadata['status'] = 'running' metadata['tmux_session'] = tmux_name metadata['started_at'] = datetime.now().isoformat() (session_dir / 'metadata.json').write_text( json.dumps(metadata, indent=2) ) print(f"✓ Started session {session_id}") print(f" Attach with: tmux attach -t {tmux_name}") print(f" Logs: {log_file}") return True def find_session_dir(self, session_id): """Find session directory by ID""" for path in self.workspace.glob(f"*_{session_id}"): if path.is_dir(): return path return None def get_session_status(self, session_id): """Get current session status""" session_dir = self.find_session_dir(session_id) if not session_dir: return None metadata = json.loads((session_dir / 'metadata.json').read_text()) # Check if tmux session is still running if 'tmux_session' in metadata: result = subprocess.run( ['tmux', 'has-session', '-t', metadata['tmux_session']], capture_output=True ) metadata['tmux_running'] = (result.returncode == 0) # Get log size log_file = session_dir / 'session.log' if log_file.exists(): metadata['log_size'] = log_file.stat().st_size return metadata def list_sessions(self): """List all sessions""" sessions = [] for session_dir in sorted(self.workspace.glob("*_*")): if (session_dir / 'metadata.json').exists(): metadata = json.loads( (session_dir / 'metadata.json').read_text() ) sessions.append(metadata) return sessions def compare_sessions(self, session_ids): """Compare multiple sessions""" comparisons = {} for sid in session_ids: session_dir = self.find_session_dir(sid) if not session_dir: continue metadata = json.loads( (session_dir / 'metadata.json').read_text() ) # Get output/log log_file = session_dir / 'session.log' output = log_file.read_text() if log_file.exists() else "" comparisons[sid] = { 'name': metadata['name'], 'created': metadata['created_at'], 'output_length': len(output), 'output_preview': output[:500] if output else "", 'metadata': metadata } return comparisons def main(): """CLI Interface""" if len(sys.argv) < 2: print(""" Usage: session_manager.py create <name> <prompt> session_manager.py start <session_id> session_manager.py status <session_id> session_manager.py list session_manager.py compare <id1> <id2> [id3...] Examples: session_manager.py create numpy-opt "Optimize using NumPy" session_manager.py start a3f9c2b4 session_manager.py compare a3f9c2b4 7d2e8f1a """) return mgr = SessionManager() command = sys.argv[1] if command == 'create': name = sys.argv[2] prompt = ' '.join(sys.argv[3:]) session_id, session_dir = mgr.create_branch(name, prompt) print(f"\nNext: session_manager.py start {session_id}") elif command == 'start': session_id = sys.argv[2] mgr.start_session(session_id) elif command == 'status': session_id = sys.argv[2] status = mgr.get_session_status(session_id) if status: print(json.dumps(status, indent=2)) else: print(f"Session not found: {session_id}") elif command == 'list': sessions = mgr.list_sessions() print(f"\n📋 Sessions ({len(sessions)}):\n") for s in sessions: status_emoji = "🟢" if s.get('status') == 'running' else "⚪" print(f"{status_emoji} {s['id']} - {s['name']}") print(f" Created: {s['created_at']}") print() elif command == 'compare': session_ids = sys.argv[2:] comparisons = mgr.compare_sessions(session_ids) print("\n🔍 Session Comparison:\n") for sid, data in comparisons.items(): print(f"Session: {sid} ({data['name']})") print(f" Output length: {data['output_length']} chars") print(f" Preview: {data['output_preview'][:100]}...") print() else: print(f"Unknown command: {command}") if __name__ == '__main__': main() EOFPYTHON chmod +x session_manager.py ``` ### 4.6 System Prompt für Meta-Clawd ```bash cat > system_prompt.md << 'EOF' # META-CLAWD ORCHESTRATOR - System Prompt You are Meta-Clawd, an AI orchestrator that manages context, coordinates agents, and enhances prompts. ## Your Capabilities ### 1. Context Management - Gather system context (git, files, shell history) - Search knowledge base using RAG - Index new information automatically - Track conversation history ### 2. Session Branching - Create parallel experiment sessions when uncertain - Compare results objectively - Merge best solutions - Learn from all branches ### 3. Agent Coordination - Identify when to delegate tasks - Spawn specialized agents (coding, research, debug, optimize) - Monitor progress - Synthesize results ### 4. Prompt Enhancement - Analyze user intent deeply - Add relevant context from knowledge base - Optimize prompt structure for target agents - Warn about potential issues ## Available Tools You have access to these tools via shell commands: ```bash # RAG Operations python tools/rag_engine.py search "<query>" [--type TYPE] [--limit N] python tools/rag_engine.py index <file> python tools/rag_engine.py index-dir <directory> python tools/rag_engine.py stats # Context Gathering python tools/context_gatherer.py # Returns JSON with git, files, shell, system context # Session Management python tools/session_manager.py create <name> "<prompt>" python tools/session_manager.py start <session_id> python tools/session_manager.py status <session_id> python tools/session_manager.py list python tools/session_manager.py compare <id1> <id2> ``` ## Workflow For each user request: 1. **Understand Intent** - What does the user REALLY want? - Are they asking the right question? 2. **Gather Context** - Run context_gatherer.py for system state - Search RAG for relevant past work - Check git status if relevant 3. **Decide Strategy** - Can I solve this directly? → Do it - Am I uncertain? → Create branch sessions to explore - Is this complex? → Delegate to specialized agent - Do I need more info? → Ask clarifying questions 4. **Execute** - Use appropriate tools - Monitor progress - Collect results 5. **Learn & Update** - Index important conversations - Note successful patterns - Update knowledge base ## Personality - **Proactive:** Don't just answer, improve the question - **Transparent:** Explain your reasoning - **Thorough:** Use tools aggressively, don't guess - **Collaborative:** Work WITH the user, not FOR them - **Learning:** Every interaction improves future ones ## Examples **User:** "Fix my code" **Bad Response:** "Please share your code." **Good Response:** Let me gather context first... ```bash python tools/context_gatherer.py ``` I see you're in `my-project/` working on `utils.py`. Recent git changes show modifications to `calculate_metrics()`. Let me search for related issues: ```bash python tools/rag_engine.py search "bug calculate_metrics" --type code ``` Found 3 similar issues you've solved before. The pattern suggests this might be a performance issue, not a bug. Should I: A) Analyze the current code and suggest fixes B) Create parallel sessions to test different optimization approaches C) Search for the specific error message first --- **User:** "Optimize this function" **Your Response:** I'll create two parallel approaches: ```bash python tools/session_manager.py create numpy-approach "Optimize calculate_metrics() using NumPy vectorization. Context: [current code]" python tools/session_manager.py create cython-approach "Optimize calculate_metrics() using Cython. Context: [current code]" ``` Both sessions running. I'll compare results in ~5 minutes. Meanwhile, I'm indexing this conversation: ```bash echo "[conversation]" | python tools/rag_engine.py index conversation ``` --- ## Remember - Always use tools, don't rely on memory alone - When in doubt, branch and experiment - Index valuable knowledge for future use - Be honest about uncertainty - Focus on user's actual goal, not literal request You are not just an assistant - you are a meta-cognitive layer that makes the entire AI system smarter. EOF ``` ### 4.7 Tools Configuration ```bash cat > tools_config.yaml << 'EOF' tools: rag_search: command: "cd {workspace}/tools && python rag_engine.py search '{query}' --limit {limit}" description: "Search knowledge base semantically" parameters: - query: string (required) - limit: integer (default: 5) rag_index: command: "cd {workspace}/tools && python rag_engine.py index '{path}'" description: "Index a file into knowledge base" parameters: - path: string (required) rag_index_dir: command: "cd {workspace}/tools && python rag_engine.py index-dir '{directory}'" description: "Index entire directory" parameters: - directory: string (required) context_gather: command: "cd {workspace}/tools && python context_gatherer.py" description: "Gather current system context (git, files, shell)" parameters: [] session_create: command: "cd {workspace}/tools && python session_manager.py create '{name}' '{prompt}'" description: "Create new branch session" parameters: - name: string (required) - prompt: string (required) session_start: command: "cd {workspace}/tools && python session_manager.py start {session_id}" description: "Start a created session" parameters: - session_id: string (required) session_compare: command: "cd {workspace}/tools && python session_manager.py compare {session_ids}" description: "Compare multiple sessions" parameters: - session_ids: string (space-separated IDs) variables: workspace: "~/ai-workspace/meta-orchestrator" EOF ``` --- ## 5. KONFIGURATION ### 5.1 Environment Setup ```bash # .envrc (optional, wenn du direnv nutzt) cat > ~/.config/meta-clawd/env << 'EOF' export META_CLAWD_WORKSPACE=~/ai-workspace/meta-orchestrator export META_CLAWD_AUTO_INDEX=true export META_CLAWD_MIN_SIMILARITY=0.6 EOF # Oder in ~/.bashrc / ~/.zshrc cat >> ~/.bashrc << 'EOF' # Meta-Clawd Shortcuts alias meta-clawd='cd ~/ai-workspace/meta-orchestrator && clawd --system-prompt system_prompt.md' alias rag-search='python ~/ai-workspace/meta-orchestrator/tools/rag_engine.py search' alias rag-index='python ~/ai-workspace/meta-orchestrator/tools/rag_engine.py index' alias ctx='python ~/ai-workspace/meta-orchestrator/tools/context_gatherer.py' EOF source ~/.bashrc ``` ### 5.2 Initial Index Population ```bash cd ~/ai-workspace/meta-orchestrator # Indexiere deine bestehenden Projekte python tools/rag_engine.py index-dir ~/projects --recursive # Indexiere Dokumentation (falls vorhanden) python tools/rag_engine.py index-dir ~/Documents/notes # Stats anschauen python tools/rag_engine.py stats ``` --- ## 6. ERSTE NUTZUNG ### 6.1 Meta-Clawd starten ```bash cd ~/ai-workspace/meta-orchestrator # Starte Meta-Clawd mit System Prompt clawd --system-prompt system_prompt.md # Oder mit Alias meta-clawd ``` ### 6.2 Erster Test ``` [You] Hey Meta-Clawd! Teste deine Tools: 1. Gather system context 2. Search knowledge base for "optimization" 3. Show me stats [Meta-Clawd sollte antworten und Tools nutzen...] ``` ### 6.3 Beispiel-Session: Code-Optimierung ``` [You] Ich habe eine langsame Funktion in ~/projects/my-app/utils.py:process_data() Die läuft auf 10K Zeilen und dauert 5 Sekunden. Zu langsam! [Meta-Clawd] Let me gather context and search for similar optimization work... [Führt aus:] 1. python tools/context_gatherer.py 2. python tools/rag_engine.py search "slow function optimization" --type code 3. cat ~/projects/my-app/utils.py | grep -A 50 "def process_data" [Analysiert und schlägt vor:] I found 3 previous optimizations with similar patterns. Most effective was switching from pandas to NumPy for large datasets. Should I: A) Create 2 branch sessions (NumPy vs Polars) and compare results B) Profile the function first to find exact bottleneck C) Show you the previous optimization code for reference [You] A - create branches [Meta-Clawd] Creating parallel optimization attempts... [Führt aus:] python tools/session_manager.py create numpy-opt "Optimize process_data() using NumPy. Current bottleneck: DataFrame operations. Context: [function code]" python tools/session_manager.py create polars-opt "Optimize process_data() using Polars. Current bottleneck: DataFrame operations. Context: [function code]" ✓ Created 2 sessions ✓ Both running in tmux You can monitor with: tmux attach -t session-<id> I'll compare results when both finish. Estimated time: 5-10 minutes. ``` --- ## 7. ERWEITERTE FEATURES ### 7.1 Auto-Indexing Watcher ```bash cat > ~/ai-workspace/meta-orchestrator/tools/auto_indexer.py << 'EOFPYTHON' #!/usr/bin/env python3 """Auto-index changed files""" from watchdog.observers import Observer from watchdog.events import FileSystemEventHandler from pathlib import Path import time import sys # Import RAG engine sys.path.insert(0, str(Path(__file__).parent)) from rag_engine import RAGEngine class AutoIndexer(FileSystemEventHandler): def __init__(self, watch_dirs, extensions): self.rag = RAGEngine() self.extensions = set(extensions) print(f"Auto-indexer watching: {watch_dirs}") print(f"Extensions: {extensions}") def on_modified(self, event): if event.is_directory: return path = Path(event.src_path) if path.suffix in self.extensions: print(f"Indexing: {path}") try: self.rag.index_code_file(path) self.rag.save_index() except Exception as e: print(f"Error indexing {path}: {e}") def main(): watch_dirs = [ Path.home() / "projects", Path.home() / "ai-workspace" ] extensions = ['.py', '.js', '.ts', '.md', '.txt'] indexer = AutoIndexer(watch_dirs, extensions) observer = Observer() for directory in watch_dirs: if directory.exists(): observer.schedule(indexer, str(directory), recursive=True) print(f"✓ Watching {directory}") observer.start() print("\n🔍 Auto-indexer running. Press Ctrl+C to stop.") try: while True: time.sleep(1) except KeyboardInterrupt: observer.stop() observer.join() if __name__ == '__main__': # Install: pip install watchdog try: from watchdog.observers import Observer from watchdog.events import FileSystemEventHandler main() except ImportError: print("Error: watchdog not installed. Run: pip install watchdog") EOFPYTHON chmod +x tools/auto_indexer.py # Als Background-Service laufen lassen # In systemd oder einfach: nohup python tools/auto_indexer.py > auto_indexer.log 2>&1 & ``` ### 7.2 Web Dashboard (Optional) ```bash cat > ~/ai-workspace/meta-orchestrator/tools/dashboard.py << 'EOFPYTHON' #!/usr/bin/env python3 """Simple web dashboard for Meta-Clawd""" from flask import Flask, render_template_string, jsonify import sys from pathlib import Path sys.path.insert(0, str(Path(__file__).parent)) from rag_engine import RAGEngine from session_manager import SessionManager app = Flask(__name__) rag = RAGEngine() sessions = SessionManager() TEMPLATE = """ <!DOCTYPE html> <html> <head> <title>Meta-Clawd Dashboard</title> <style> body { font-family: system-ui; margin: 20px; background: #1a1a2e; color: #eee; } .card { background: #16213e; padding: 20px; margin: 20px 0; border-radius: 8px; } h1 { color: #00d9ff; } .stat { display: inline-block; margin: 10px 20px; } table { width: 100%; border-collapse: collapse; } th, td { padding: 10px; text-align: left; border-bottom: 1px solid #0f3460; } th { color: #00d9ff; } </style> </head> <body> <h1>🎯 Meta-Clawd Dashboard</h1> <div class="card"> <h2>📊 Knowledge Base</h2> <div class="stat">Total Documents: <strong id="total-docs">-</strong></div> <div class="stat">Code Files: <strong id="code-docs">-</strong></div> <div class="stat">Conversations: <strong id="conv-docs">-</strong></div> </div> <div class="card"> <h2>🔄 Active Sessions</h2> <table id="sessions-table"> <tr><th>ID</th><th>Name</th><th>Status</th><th>Created</th></tr> </table> </div> <script> async function loadStats() { const stats = await fetch('/api/stats').then(r => r.json()); document.getElementById('total-docs').textContent = stats.total_documents; document.getElementById('code-docs').textContent = stats.by_type.code || 0; document.getElementById('conv-docs').textContent = stats.by_type.conversation || 0; } async function loadSessions() { const sessions = await fetch('/api/sessions').then(r => r.json()); const table = document.getElementById('sessions-table'); sessions.forEach(s => { const row = table.insertRow(); row.innerHTML = `<td>${s.id}</td><td>${s.name}</td><td>${s.status}</td><td>${s.created_at}</td>`; }); } loadStats(); loadSessions(); setInterval(loadStats, 10000); // Refresh every 10s </script> </body> </html> """ @app.route('/') def index(): return render_template_string(TEMPLATE) @app.route('/api/stats') def api_stats(): return jsonify(rag.stats()) @app.route('/api/sessions') def api_sessions(): return jsonify(sessions.list_sessions()) if __name__ == '__main__': print("🌐 Dashboard running at http://localhost:5555") app.run(host='0.0.0.0', port=5555) EOFPYTHON chmod +x tools/dashboard.py # Start dashboard python tools/dashboard.py # → Open http://localhost:5555 ``` --- ## 8. TROUBLESHOOTING ### 8.1 Häufige Probleme **Problem:** RAG Engine findet keine Ergebnisse ```bash # Check index python tools/rag_engine.py stats # Wenn leer: Re-index python tools/rag_engine.py index-dir ~/projects ``` **Problem:** tmux Session startet nicht ```bash # Check tmux tmux list-sessions # Falls Fehler: Installiere tmux sudo apt install tmux # Debian/Ubuntu sudo pacman -S tmux # Arch brew install tmux # macOS ``` **Problem:** Context Gatherer findet nichts ```bash # Check git cd ~/projects/your-project git status # Check lsof lsof -u $USER | head ``` **Problem:** Embedding Model lädt nicht ```bash # Check Internet connection ping huggingface.co # Manuell herunterladen python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('all-MiniLM-L6-v2')" ``` ### 8.2 Logs & Debugging ```bash # Enable verbose logging export META_CLAWD_DEBUG=1 # Check RAG database sqlite3 ~/ai-workspace/meta-orchestrator/knowledge.db "SELECT COUNT(*) FROM documents;" # Check FAISS index python -c "import faiss; idx = faiss.read_index('faiss.index'); print(f'Vectors: {idx.ntotal}')" # Session logs cat ~/ai-workspace/sessions/*/session.log ``` --- ## 9. MAINTENANCE & UPDATES ### 9.1 Regular Maintenance ```bash # Weekly: Cleanup old sessions (>30 days) find ~/ai-workspace/sessions -type d -mtime +30 -exec rm -rf {} \; # Monthly: Optimize database sqlite3 ~/ai-workspace/meta-orchestrator/knowledge.db "VACUUM;" # Monthly: Rebuild index (if corrupted) cd ~/ai-workspace/meta-orchestrator mv faiss.index faiss.index.backup python tools/rag_engine.py index-dir ~/projects ``` ### 9.2 Backups ```bash # Backup script cat > ~/ai-workspace/backup.sh << 'EOF' #!/bin/bash BACKUP_DIR=~/backups/meta-clawd DATE=$(date +%Y%m%d) mkdir -p $BACKUP_DIR # Backup knowledge base tar -czf $BACKUP_DIR/knowledge-$DATE.tar.gz \ ~/ai-workspace/meta-orchestrator/knowledge.db \ ~/ai-workspace/meta-orchestrator/faiss.index* # Keep only last 7 backups ls -t $BACKUP_DIR/knowledge-*.tar.gz | tail -n +8 | xargs rm -f echo "✓ Backup complete: $BACKUP_DIR/knowledge-$DATE.tar.gz" EOF chmod +x ~/ai-workspace/backup.sh # Add to cron (weekly) crontab -e # Add line: # 0 2 * * 0 ~/ai-workspace/backup.sh ``` ### 9.3 Updates ```bash # Update dependencies cd ~/ai-workspace/meta-orchestrator source venv/bin/activate pip install --upgrade sentence-transformers faiss-cpu transformers torch # Pull latest tools (if you have them in git) git pull origin main ``` --- ## 10. USAGE PATTERNS & BEST PRACTICES ### 10.1 When to Use What | Situation | Tool | Why | |-----------|------|-----| | "How did I solve X before?" | `rag_search` | Finds past solutions | | "What am I working on?" | `context_gather` | Current state | | "Try 2 approaches" | `session_create` (2x) | Parallel experiments | | "This is complex" | Delegate to agent | Specialized handling | | "Need to remember this" | Index conversation | Future reference | ### 10.2 Workflow Examples **Research Task:** ``` 1. rag_search for existing knowledge 2. If gaps: create research-agent session 3. Agent gathers info 4. Index results 5. Synthesize answer ``` **Bug Fixing:** ``` 1. context_gather (what changed recently?) 2. rag_search for similar bugs 3. If found: apply previous fix 4. If new: create debug-agent session 5. Index solution ``` **Optimization:** ``` 1. Profile current code 2. rag_search for optimization patterns 3. Create 2-3 branch sessions (different approaches) 4. Compare results 5. Merge best 6. Index winning approach ``` --- ## 11. KOSTEN & RESSOURCEN ### 11.1 Disk Usage ``` Component Size ───────────────────────────── RAG Engine binary ~80MB FAISS Index ~1MB per 1000 docs SQLite DB ~500KB per 1000 docs Session logs ~10MB per session Total (typical) ~500MB - 2GB ``` ### 11.2 RAM Usage ``` Component RAM ───────────────────────────── Sentence Transformers ~500MB FAISS Index (loaded) ~Size of index file Python overhead ~200MB Total ~1GB ``` ### 11.3 CPU Usage ``` Operation CPU Time ───────────────────────────────── Embedding 1 doc ~10-50ms Search 10K docs ~20-100ms Index 1000 files ~30-60s Context gathering ~100-500ms ``` ### 11.4 Financial Costs ``` Item Cost ────────────────────────────── All software $0 (open source) Electricity (idle) ~$0.01/day Electricity (active) ~$0.05/day API calls (Clawd) Variable (depends on provider) Total recurring ~$1-3/month ``` --- ## 12. NEXT STEPS ### 12.1 Getting Started Checklist - [ ] Workspace created - [ ] Python environment setup - [ ] RAG Engine installed & tested - [ ] Context Gatherer working - [ ] Session Manager working - [ ] System prompt configured - [ ] Initial index populated - [ ] First Meta-Clawd session successful ### 12.2 First Week Goals - [ ] Index all your existing projects - [ ] Use RAG search 10+ times - [ ] Create at least 2 branch sessions - [ ] Compare session results - [ ] Index 3+ conversations - [ ] Customize system prompt to your style ### 12.3 Advanced Goals - [ ] Setup auto-indexer - [ ] Create custom specialized agent - [ ] Integrate with your CI/CD - [ ] Build personal knowledge graph - [ ] Fine-tune embedding model on your data - [ ] Multi-modal indexing (images, PDFs) --- ## 13. ARCHITECTURE DECISIONS ### 13.1 Why These Technologies? **sentence-transformers:** - Lightweight (~80MB) - Fast (CPU-friendly) - Good quality embeddings - No API needed **FAISS:** - Extremely fast similarity search - Scales to millions of vectors - Low memory footprint - Battle-tested (Meta/Facebook) **SQLite:** - Zero config - Single file - ACID compliant - Fast for metadata **tmux:** - Ubiquitous - Persistent sessions - Easy monitoring - Scriptable **Python:** - Ecosystem for ML/AI - Easy to extend - Cross-platform - Readable ### 13.2 Alternative Approaches | Component | Current | Alternative | Tradeoff | |-----------|---------|-------------|----------| | Embeddings | sentence-transformers | OpenAI API | Cost vs Quality | | Vector DB | FAISS | ChromaDB, Pinecone | Simplicity vs Features | | Sessions | tmux | Docker, k8s | Complexity vs Scalability | | Storage | SQLite | PostgreSQL | Setup vs Power | | Orchestrator | Clawd | LangChain | Custom vs Framework | --- ## ZUSAMMENFASSUNG Du hast jetzt: ✅ **Komplettes Meta-Clawd System** - RAG Engine für semantische Suche - Session Manager für parallele Experimente - Context Gatherer für System-State - Alles orchestriert durch Nano-Clawd ✅ **Installation in ~2-4 Stunden** - Copy-paste friendly - Schritt-für-Schritt getestet - Minimal dependencies ✅ **$0 laufende Kosten** - Alles lokal - Open Source - Kein API-Lock-in ✅ **Sofort produktiv** - Use cases klar - Workflows dokumentiert - Beispiele gegeben ✅ **Erweiterbar** - Modular design - Easy to customize - Community-friendly **Nächster Schritt:** Fang einfach an! Kopiere die Commands, führe sie aus, und in 2-3 Stunden hast du ein laufendes System. **Fragen? Issues? Improvements?** → Das System ist so designed dass es MIT dir lernt und wächst. Viel Erfolg! 🚀 --- **Dokument-Version:** 1.0 **Letzte Änderung:** 2025-01-15 **Maintainer:** [Your Name] **License:** MIT / CC-BY-4.0 **Repository:** [Optional: GitHub Link]


--- Datei: /home/joe/Dokumente/projects/Apps/partner/meta-orchestrator/sessions/test-session_b64d19f5/prompt.txt (Relevanz: 0.48) ---
USER INTENT / PROMPT:
Optimiere meine Funktion

==================================================
AUTOMATISCH EINGEBETTETER KONTEXT (ML-gesteuert):
==================================================
--- Datei: /home/joe/Dokumente/projects/Apps/partner/meta-orchestrator/tools/rag_engine.py (Relevanz: 0.37) ---
#!/usr/bin/env python3
"""
RAG Engine for Meta-Clawd
- Semantic Search & Indexing
"""
import sys
import os
import json
import sqlite3
import numpy as np
import faiss
from pathlib import Path
from datetime import datetime
from sentence_transformers import SentenceTransformer

# Configuration
BASE_DIR = Path(__file__).resolve().parent.parent
KNOWLEDGE_DIR = BASE_DIR / "knowledge-base"
KNOWLEDGE_DIR.mkdir(parents=True, exist_ok=True)

DB_PATH = KNOWLEDGE_DIR / "knowledge.db"
INDEX_PATH = KNOWLEDGE_DIR / "faiss.index"
MODEL_NAME = 'all-MiniLM-L6-v2'

class RAGEngine:
    def __init__(self):
        self.model = SentenceTransformer(MODEL_NAME)
        self._init_db()
        self._init_index()

    def _init_db(self):
        """Initialize SQLite metadata storage"""
        self.db = sqlite3.connect(DB_PATH)
        self.db.execute("""
            CREATE TABLE IF NOT EXISTS documents (
                id INTEGER PRIMARY KEY,
                path TEXT UNIQUE,
                type TEXT,
                content TEXT,
                metadata TEXT,
                created_at TIMESTAMP,
                updated_at TIMESTAMP
            )
        """)
        self.db.commit()

    def _init_index(self):
        """Initialize FAISS vector index"""
        if INDEX_PATH.exists():
            self.index = faiss.read_index(str(INDEX_PATH))
        else:
            # Dimension depends on model (384 for all-MiniLM-L6-v2)
            self.index = faiss.IndexFlatL2(384)

    def save_index(self):
        """Persist index to disk"""
        faiss.write_index(self.index, str(INDEX_PATH))

    def index_document(self, content, doc_type, metadata=None, path=None):
        """Index a single document"""
        if not content.strip():
            return None

        # Generate embedding
        vector = self.model.encode([content])[0]
        
        # Normalize vector for cosine similarity (if using L2 index as approx)
        faiss.normalize_L2(vector.reshape(1, -1))

        # Update Database
        now = datetime.now()
        meta_json = json.dumps(metadata or {})
        
        try:
            cursor = self.db.execute("""
                INSERT INTO documents (path, type, content, metadata, created_at, updated_at)
                VALUES (?, ?, ?, ?, ?, ?)
                ON CONFLICT(path) DO UPDATE SET
                    content=excluded.content,
                    metadata=excluded.metadata,
                    updated_at=excluded.updated_at
            """, (path or f"doc_{self.index.ntotal}", doc_type, content, meta_json, now, now))
            
            doc_id = cursor.lastrowid
            
            # Add to FAISS (assuming ID matches rowid roughly, but for simplicity we append)
            # Note: A real production system would manage IDs more strictly to allow updates/deletes in FAISS
            self.index.add(np.array([vector], dtype=np.float32))
            
            self.db.commit()
            return doc_id
        except Exception as e:
            print(f"Error indexing: {e}")
            return None

    def search(self, query, k=5, doc_type=None):
        """Semantic search"""
        vector = self.model.encode([query])[0]
        faiss.normalize_L2(vector.reshape(1, -1))
        
        # Search
        distances, indices = self.index.search(np.array([vector], dtype=np.float32), k)
        
        results = []
        for i, idx in enumerate(indices[0]):
            if idx == -1: continue
            
            # Retrieve metadata from DB (This is a simplification. 
            # In a robust system, we need a mapping from FAISS ID to DB ID)
            # Here we assume FAISS ID == DB ID - 1 (0-based vs 1-based)
            # WARNING: This desyncs if we update documents. 
            # For v1.0 we stick to this or fetch by content hash? 
            # Let's just fetch by LIMIT/OFFSET strategy if we had a mapping,
            # but for now let's query by rowid.
            
            rowid = int(idx) + 1 
            cursor = self.db.execute("SELECT * FROM documents WHERE rowid=?", (rowid,))
            row = cursor.fetchone()
            
            if row:
                # Filter by type if requested
                if doc_type and row[2] != doc_type:
                    continue
                    
                results.append({
                    'id': row[0],
                    'path': row[1],
                    'type': row[2],
                    'content': row[3],
                    'metadata': json.loads(row[4]),
                    'similarity': float(1 / (1 + distances[0][i])) # Rough conversion L2->Sim
                })
        
        return results

    def index_code_file(self, file_path):
        """Index a code file"""
        path = Path(file_path)
        if not path.exists():
            print(f"File not found: {path}")
            return None
            
        try:
            content = path.read_text(encoding='utf-8')
            return self.index_document(
                content=content,
                doc_type='code',
                metadata={
                    'file_path': str(path.absolute()),
                    'file_name': path.name,
                    'language': path.suffix[1:] if path.suffix else 'unknown',
                    'size': len(content)
                },
                path=str(path.absolute())
            )
        except Exception as e:
            print(f"⚠ Error indexing {file_path}: {e}")
            return None

    def index_directory(self, directory, extensions=None, recursive=True):
        """Index all code files in a directory"""
        if extensions is None:
            extensions = {'.py', '.js', '.ts', '.jsx', '.tsx', '.rs', '.go', '.java', '.c', '.cpp', '.h', '.hpp', '.md', '.txt'}
            
        dir_path = Path(directory)
        indexed = 0
        skipped = 0
        
        pattern = '**/*' if recursive else '*'
        
        for file_path in dir_path.glob(pattern):
            if file_path.is_file() and file_path.suffix in extensions:
                # Skip common non-code files, hidden dirs, and environments
                ignore_dirs = {'venv', 'env', 'partnerenv', 'node_modules', '__pycache__', 'dist', 'build', '.maestro', 'partnerenv'}
                if any(part.startswith('.') or part in ignore_dirs for part in file_path.parts):
                    skipped += 1
                    continue
                    
                if self.index_code_file(file_path):
                    indexed += 1
                    if indexed % 10 == 0:
                        print(f"  Indexed {indexed} files...")
                else:
                    skipped += 1
        
        self.save_index()
        print(f"✓ Indexed {indexed} files, skipped {skipped}")
        return indexed

    def stats(self):
        """Get index statistics"""
        cursor = self.db.execute("""
            SELECT type, COUNT(*) as count FROM documents GROUP BY type
        """)
        stats = {
            'total_documents': self.index.ntotal,
            'by_type': {row[0]: row[1] for row in cursor.fetchall()}
        }
        return stats

def main():
    """CLI Interface"""
    if len(sys.argv) < 2:
        print("""
        Usage:
          rag_engine.py search <query> [--type TYPE] [--limit N]
          rag_engine.py index <path> [--type TYPE]
          rag_engine.py index-dir <directory> [--recursive]
          rag_engine.py stats
        """)
        return

    rag = RAGEngine()
    command = sys.argv[1]

    if command == 'search':
        query = ' '.join(arg for arg in sys.argv[2:] if not arg.startswith('--'))
        doc_type = None
        limit = 5
        
        for i, arg in enumerate(sys.argv):
            if arg == '--type' and i + 1 < len(sys.argv):
                doc_type = sys.argv[i + 1]
            elif arg == '--limit' and i + 1 < len(sys.argv):
                limit = int(sys.argv[i + 1])
                
        results = rag.search(query, k=limit, doc_type=doc_type)
        
        if not results:
            print("No results found.")
            return
            
        print(f"\nTop {len(results)} results for: {query}\n")
        for i, result in enumerate(results, 1):
            print(f"{i}. [{result['type']}] (sim: {result['similarity']:.2f})")
            print(f"   Path: {result['path']}")
            content_preview = result['content'][:200].replace('\n', ' ')
            print(f"   {content_preview}...")
            print()

    elif command == 'index':
        if len(sys.argv) < 3:
            print("Error: Missing file path")
            return
        file_path = sys.argv[2]
        doc_id = rag.index_code_file(file_path)
        if doc_id:
            print(f"✓ Indexed as document #{doc_id}")
            rag.save_index()

    elif command == 'index-dir':
        if len(sys.argv) < 3:
            print("Error: Missing directory path")
            return
        directory = sys.argv[2]
        recursive = '--recursive' in sys.argv
        rag.index_directory(directory, recursive=recursive)

    elif command == 'stats':
        stats = rag.stats()
        print("\n📊 RAG Index Statistics\n")
        print(f"Total documents: {stats['total_documents']}")
        print("\nBy type:")
        for doc_type, count in stats['by_type'].items():
            print(f"  {doc_type}: {count}")
        print()

    else:
        print(f"Unknown command: {command}")

if __name__ == '__main__':
    main()


--- Datei: /home/joe/Dokumente/projects/Apps/partner/idee.txt (Relevanz: 0.37) ---
# 🏗️ META-CLAWD ORCHESTRATOR - KOMPLETTER BAUPLAN **Version:** 1.0 **Datum:** 2025-01-15 **Geschätzter Aufwand:** 2-4 Stunden Setup, dann sofort nutzbar **Kosten:** $0 (alles lokal) --- ## 📋 INHALTSVERZEICHNIS 1. [Übersicht & Architektur](#1-übersicht--architektur) 2. [Voraussetzungen](#2-voraussetzungen) 3. [Komponenten-Details](#3-komponenten-details) 4. [Installation Schritt-für-Schritt](#4-installation-schritt-für-schritt) 5. [Konfiguration](#5-konfiguration) 6. [Erste Nutzung](#6-erste-nutzung) 7. [Erweiterte Features](#7-erweiterte-features) 8. [Troubleshooting](#8-troubleshooting) 9. [Maintenance & Updates](#9-maintenance--updates) --- ## 1. ÜBERSICHT & ARCHITEKTUR ### 1.1 Was wird gebaut? ``` ┌────────────────────────────────────────────────────────────────┐ │ META-CLAWD SYSTEM │ ├────────────────────────────────────────────────────────────────┤ │ │ │ ┌──────────────────────────────────────────────────────────┐ │ │ │ Layer 1: Meta-Orchestrator (Nano-Clawd Session) │ │ │ │ - Hauptsteuerung │ │ │ │ - Natürliche Sprach-Interface │ │ │ │ - Tool-Orchestrierung │ │ │ └──────────────────┬───────────────────────────────────────┘ │ │ │ │ │ ┌──────────────────┴───────────────────────────────────────┐ │ │ │ Layer 2: Core Tools │ │ │ │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │ │ │ │ │ RAG Engine │ │ Session │ │ Context │ │ │ │ │ │ │ │ Manager │ │ Gatherer │ │ │ │ │ │ - Index │ │ - Branching │ │ - Git │ │ │ │ │ │ - Search │ │ - Compare │ │ - Files │ │ │ │ │ │ - Embed │ │ - Merge │ │ - System │ │ │ │ │ └─────────────┘ └─────────────┘ └─────────────┘ │ │ │ └──────────────────────────────────────────────────────────┘ │ │ │ │ ┌──────────────────────────────────────────────────────────┐ │ │ │ Layer 3: Storage & State │ │ │ │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │ │ │ │ │ SQLite DB │ │ FAISS Index │ │ Sessions/ │ │ │ │ │ │ (Metadata) │ │ (Vectors) │ │ Logs │ │ │ │ │ └─────────────┘ └─────────────┘ └─────────────┘ │ │ │ └──────────────────────────────────────────────────────────┘ │ │ │ │ ┌──────────────────────────────────────────────────────────┐ │ │ │ Layer 4: Agent Pool (dynamisch) │ │ │ │ [Coding] [Research] [Debug] [Optimize] [...] │ │ │ └──────────────────────────────────────────────────────────┘ │ └────────────────────────────────────────────────────────────────┘ ``` ### 1.2 Datenfluss ``` User Input │ ▼ Meta-Clawd empfängt │ ├─► Context Gatherer holt System-Kontext │ │ │ ├─► Git Status │ ├─► Offene Dateien │ ├─► Recent Commands │ └─► Environment Vars │ ├─► RAG Engine sucht relevanten Kontext │ │ │ ├─► Semantic Search in Index │ ├─► Findet ähnliche Probleme │ └─► Holt passende Code-Beispiele │ ├─► Meta-Clawd analysiert & entscheidet │ │ │ ├─► Selbst lösen? → Direct Action │ │ │ ├─► Unsicher? → Branching │ │ │ │ │ ├─► Session A: Approach X │ │ ├─► Session B: Approach Y │ │ └─► Compare & Merge Best │ │ │ └─► Komplex? → Delegation │ │ │ ├─► Start Specialized Agent │ ├─► Monitor Progress │ └─► Collect Results │ └─► Output zurück an User │ └─► Index Update (lernt aus Session) ``` ### 1.3 Dateisystem-Struktur ``` ~/ai-workspace/ ├── meta-orchestrator/ # Haupt-Meta-Session │ ├── system_prompt.md # Instruktionen für Meta-Clawd │ ├── tools_config.yaml # Tool-Definitionen │ ├── knowledge.db # SQLite Metadata │ ├── faiss.index # Vector Index │ ├── faiss.index.metadata # Index Metadata │ ├── session_history.jsonl # Conversation Logs │ └── tools/ # Tool Scripts │ ├── rag_engine.py │ ├── session_manager.py │ ├── context_gatherer.py │ └── agent_spawner.py │ ├── sessions/ # Branch-Sessions │ ├── experiment-numpy_a3f9c2/ │ ├── experiment-cython_7d2e8f/ │ └── debug-session_4b6a1c/ │ ├── agents/ # Specialized Agents │ ├── coding-agent/ │ ├── research-agent/ │ └── debug-agent/ │ └── knowledge-base/ # Raw Indexed Content ├── conversations/ ├── code-snippets/ ├── documentation/ └── outputs/ ``` --- ## 2. VORAUSSETZUNGEN ### 2.1 Hardware **Minimum:** - CPU: 4 Cores - RAM: 8GB - Disk: 10GB frei - GPU: Keine nötig (CPU-Inference ausreichend) **Empfohlen:** - CPU: 8+ Cores - RAM: 16GB+ - Disk: 50GB+ SSD - GPU: RX 6800 XT / RTX 3060+ (optional, für schnelleres Embedding) ### 2.2 Software **Basis:** ```bash # OS: Linux (Ubuntu 22.04+, Arch, Fedora) oder macOS # Shell: bash/zsh # Python: 3.10+ # Git: 2.30+ # tmux: 3.0+ (für Session-Management) ``` **Python-Packages:** ```txt sentence-transformers>=2.2.0 faiss-cpu>=1.7.4 # oder faiss-gpu wenn CUDA/ROCm transformers>=4.30.0 torch>=2.0.0 flask>=2.3.0 pyyaml>=6.0 sqlite3 # (built-in) numpy>=1.24.0 tqdm>=4.65.0 ``` **Optional:** ```txt llama-cpp-python>=0.2.0 # Für lokale Modelle (Qwen, Mistral) chromadb>=0.4.0 # Alternative zu FAISS langchain>=0.1.0 # Wenn du LangChain nutzen willst ``` ### 2.3 Existing Setup **Du brauchst bereits:** - Nano-Clawd / Claude Desktop / Vergleichbares AI-System installiert - API-Zugang (Claude, OpenAI) ODER lokales Modell (Ollama, LM Studio) **Check:** ```bash # Teste ob Clawd funktioniert clawd --version # Oder API-Test curl https://api.anthropic.com/v1/messages \ -H "x-api-key: $ANTHROPIC_API_KEY" \ -H "content-type: application/json" \ -d '{"model":"claude-3-5-sonnet-20241022","messages":[{"role":"user","content":"test"}],"max_tokens":10}' ``` --- ## 3. KOMPONENTEN-DETAILS ### 3.1 RAG Engine **Datei:** `tools/rag_engine.py` **Funktionen:** - Semantic Search über alle indexierten Inhalte - Embedding-Generierung (sentence-transformers) - Vector Storage (FAISS) - Metadata Management (SQLite) **Performance:** - Embedding: ~500 docs/sec (CPU) - Search: <50ms für 10K docs - Speicher: ~1MB per 1000 docs **Code:** (siehe unten in Installationsabschnitt) ### 3.2 Session Manager **Datei:** `tools/session_manager.py` **Funktionen:** - Branch-Erstellung (parallele Experimente) - Session-Monitoring (via tmux) - Output-Aggregation - Result-Comparison **Workflow:** ``` create_branch() → tmux new-session → clawd --prompt "{task}" → monitor output → store in session_dir/ compare_branches() → read all outputs → analyze differences → rank by criteria merge_best() → user chooses (or auto-select) → copy to main workspace → update RAG index ``` ### 3.3 Context Gatherer **Datei:** `tools/context_gatherer.py` **Sammelt:** - Git: Status, branch, recent commits, diff - Files: Offene Dateien (via lsof), recent edits - Shell: Command history, environment vars - System: Load, memory, disk usage - Time: Current time, timezone, locale **Output-Format:** ```json { "timestamp": "2025-01-15T14:30:00Z", "git": { "repo": "my-project", "branch": "feature/optimization", "status": "M utils.py\nA new_feature.py", "recent_commits": [...] }, "files": { "open": ["/home/user/project/utils.py", ...], "recent": ["/home/user/project/tests.py", ...] }, "shell": { "last_commands": ["git status", "pytest", ...], "cwd": "/home/user/project" }, "system": { "load": [0.5, 0.6, 0.7], "memory_free": "8GB", "disk_free": "120GB" } } ``` ### 3.4 Agent Spawner **Datei:** `tools/agent_spawner.py` **Agent-Typen:** - **Coding:** Code-Generierung, Refactoring - **Research:** Information-Gathering, Documentation - **Debug:** Error-Analysis, Fix-Suggestions - **Optimize:** Performance-Tuning, Profiling - **Test:** Test-Generierung, Coverage-Analysis **Spawning:** ```python def spawn_agent(agent_type, task, context): agent_dir = workspace / "agents" / f"{agent_type}-{uuid}" # Agent-spezifisches System-Prompt system_prompt = load_agent_prompt(agent_type) # Starte in tmux tmux_session = f"agent-{agent_type}-{uuid}" subprocess.run([ 'tmux', 'new-session', '-d', '-s', tmux_session, 'clawd', '--system-prompt', system_prompt, '--initial-message', task, '--context', json.dumps(context), '--output-file', agent_dir / 'output.log' ]) return tmux_session ``` --- ## 4. INSTALLATION SCHRITT-FÜR-SCHRITT ### 4.1 Workspace Erstellen ```bash # Erstelle Basis-Struktur mkdir -p ~/ai-workspace/{meta-orchestrator/tools,sessions,agents,knowledge-base/{conversations,code-snippets,documentation,outputs}} cd ~/ai-workspace/meta-orchestrator ``` ### 4.2 Python Environment ```bash # Virtual Environment python3 -m venv venv source venv/bin/activate # Dependencies cat > requirements.txt << 'EOF' sentence-transformers>=2.2.0 faiss-cpu>=1.7.4 transformers>=4.30.0 torch>=2.0.0 flask>=2.3.0 pyyaml>=6.0 numpy>=1.24.0 tqdm>=4.65.0 watchdog>=3.0.0 EOF pip install -r requirements.txt # Test python -c "from sentence_transformers import SentenceTransformer; print('✓ OK')" ``` ### 4.3 RAG Engine Installation ```bash cd ~/ai-workspace/meta-orchestrator/tools cat > rag_engine.py << 'EOFPYTHON' #!/usr/bin/env python3 """ RAG Engine - Semantic Search & Knowledge Management """ import json import sqlite3 from pathlib import Path from datetime import datetime import sys try: from sentence_transformers import SentenceTransformer import faiss import numpy as np except ImportError: print("Error: Missing dependencies. Run: pip install sentence-transformers faiss-cpu numpy") sys.exit(1) class RAGEngine: """Retrieval-Augmented Generation Engine""" def __init__(self, workspace_dir=None): if workspace_dir is None: workspace_dir = Path.home() / "ai-workspace" / "meta-orchestrator" self.workspace = Path(workspace_dir) self.db_path = self.workspace / "knowledge.db" self.index_path = self.workspace / "faiss.index" self.metadata_path = self.workspace / "faiss.index.metadata" print(f"Initializing RAG Engine at {self.workspace}") # Load embedding model (lightweight, ~80MB) print("Loading embedding model (all-MiniLM-L6-v2)...") self.model = SentenceTransformer('all-MiniLM-L6-v2') self.embedding_dim = 384 # Initialize storage self.init_db() self.init_index() print("✓ RAG Engine ready") def init_db(self): """Initialize SQLite database for metadata""" self.db = sqlite3.connect(str(self.db_path)) self.db.execute(""" CREATE TABLE IF NOT EXISTS documents ( id INTEGER PRIMARY KEY AUTOINCREMENT, type TEXT NOT NULL, content TEXT NOT NULL, metadata TEXT, embedding_id INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ) """) self.db.execute(""" CREATE INDEX IF NOT EXISTS idx_type ON documents(type) """) self.db.execute(""" CREATE INDEX IF NOT EXISTS idx_created ON documents(created_at) """) self.db.commit() def init_index(self): """Initialize or load FAISS vector index""" if self.index_path.exists(): print(f"Loading existing index from {self.index_path}") self.index = faiss.read_index(str(self.index_path)) # Load metadata if self.metadata_path.exists(): with open(self.metadata_path, 'r') as f: self.index_metadata = json.load(f) else: self.index_metadata = {'total': self.index.ntotal} else: print("Creating new FAISS index") self.index = faiss.IndexFlatL2(self.embedding_dim) self.index_metadata = {'total': 0} def index_document(self, content, doc_type, metadata=None): """Add document to index""" if not content or not content.strip(): return None # Generate embedding embedding = self.model.encode([content])[0] # Add to FAISS embedding_id = self.index.ntotal self.index.add(np.array([embedding], dtype=np.float32)) # Save to database cursor = self.db.execute(""" INSERT INTO documents (type, content, metadata, embedding_id) VALUES (?, ?, ?, ?) """, ( doc_type, content, json.dumps(metadata or {}), embedding_id )) doc_id = cursor.lastrowid self.db.commit() # Update metadata self.index_metadata['total'] = self.index.ntotal # Save index periodically (every 10 docs) if self.index.ntotal % 10 == 0: self.save_index() return doc_id def save_index(self): """Persist index to disk""" faiss.write_index(self.index, str(self.index_path)) with open(self.metadata_path, 'w') as f: json.dump(self.index_metadata, f) def search(self, query, k=5, doc_type=None, min_similarity=0.0): """Semantic search""" if self.index.ntotal == 0: return [] # Generate query embedding query_embedding = self.model.encode([query])[0] # Search in FAISS distances, indices = self.index.search( np.array([query_embedding], dtype=np.float32), min(k * 2, self.index.ntotal) # Get more, then filter ) # Retrieve documents results = [] for idx, dist in zip(indices[0], distances[0]): if idx == -1: # FAISS returns -1 for empty slots continue cursor = self.db.execute(""" SELECT id, type, content, metadata, created_at FROM documents WHERE embedding_id = ? """, (int(idx),)) row = cursor.fetchone() if row: similarity = float(1 / (1 + dist)) # Convert L2 distance to similarity if similarity < min_similarity: continue doc = { 'id': row[0], 'type': row[1], 'content': row[2], 'metadata': json.loads(row[3]) if row[3] else {}, 'created_at': row[4], 'similarity': similarity } # Filter by type if specified if doc_type is None or doc['type'] == doc_type: results.append(doc) if len(results) >= k: break return results def index_conversation(self, messages, session_name, metadata=None): """Index a complete conversation""" conversation_text = "\n\n".join([ f"{msg.get('role', 'unknown')}: {msg.get('content', '')}" for msg in messages ]) meta = metadata or {} meta.update({ 'session': session_name, 'num_messages': len(messages), 'type': 'conversation' }) return self.index_document( content=conversation_text, doc_type='conversation', metadata=meta ) def index_code_file(self, file_path): """Index a single code file""" path = Path(file_path) if not path.exists(): print(f"⚠ File not found: {file_path}") return None try: content = path.read_text(encoding='utf-8') return self.index_document( content=content, doc_type='code', metadata={ 'file_path': str(path.absolute()), 'file_name': path.name, 'language': path.suffix[1:] if path.suffix else 'unknown', 'size': len(content) } ) except Exception as e: print(f"⚠ Error indexing {file_path}: {e}") return None def index_directory(self, directory, extensions=None, recursive=True): """Index all code files in a directory""" if extensions is None: extensions = {'.py', '.js', '.ts', '.jsx', '.tsx', '.rs', '.go', '.java', '.c', '.cpp', '.h', '.hpp', '.md', '.txt'} dir_path = Path(directory) indexed = 0 skipped = 0 pattern = '**/*' if recursive else '*' for file_path in dir_path.glob(pattern): if file_path.is_file() and file_path.suffix in extensions: # Skip common non-code files if any(part.startswith('.') for part in file_path.parts): skipped += 1 continue if self.index_code_file(file_path): indexed += 1 if indexed % 10 == 0: print(f" Indexed {indexed} files...") else: skipped += 1 self.save_index() print(f"✓ Indexed {indexed} files, skipped {skipped}") return indexed def stats(self): """Get index statistics""" cursor = self.db.execute(""" SELECT type, COUNT(*) as count FROM documents GROUP BY type """) stats = { 'total_documents': self.index.ntotal, 'by_type': {row[0]: row[1] for row in cursor.fetchall()} } return stats def main(): """CLI Interface""" if len(sys.argv) < 2: print(""" Usage: rag_engine.py search <query> [--type TYPE] [--limit N] rag_engine.py index <path> [--type TYPE] rag_engine.py index-dir <directory> [--recursive] rag_engine.py stats Examples: rag_engine.py search "how to optimize performance" rag_engine.py search "bug in API" --type code rag_engine.py index my_file.py rag_engine.py index-dir ./src --recursive rag_engine.py stats """) return rag = RAGEngine() command = sys.argv[1] if command == 'search': query = ' '.join(arg for arg in sys.argv[2:] if not arg.startswith('--')) # Parse options doc_type = None limit = 5 for i, arg in enumerate(sys.argv): if arg == '--type' and i + 1 < len(sys.argv): doc_type = sys.argv[i + 1] elif arg == '--limit' and i + 1 < len(sys.argv): limit = int(sys.argv[i + 1]) results = rag.search(query, k=limit, doc_type=doc_type) if not results: print("No results found.") return print(f"\nTop {len(results)} results for: {query}\n") for i, result in enumerate(results, 1): print(f"{i}. [{result['type']}] (similarity: {result['similarity']:.2%})") # Show metadata if result['metadata']: for key, value in result['metadata'].items(): print(f" {key}: {value}") # Show content preview content_preview = result['content'][:200].replace('\n', ' ') print(f" {content_preview}...") print() elif command == 'index': if len(sys.argv) < 3: print("Error: Missing file path") return file_path = sys.argv[2] doc_id = rag.index_code_file(file_path) if doc_id: print(f"✓ Indexed as document #{doc_id}") rag.save_index() elif command == 'index-dir': if len(sys.argv) < 3: print("Error: Missing directory path") return directory = sys.argv[2] recursive = '--recursive' in sys.argv rag.index_directory(directory, recursive=recursive) elif command == 'stats': stats = rag.stats() print("\n📊 RAG Index Statistics\n") print(f"Total documents: {stats['total_documents']}") print("\nBy type:") for doc_type, count in stats['by_type'].items(): print(f" {doc_type}: {count}") print() else: print(f"Unknown command: {command}") if __name__ == '__main__': main() EOFPYTHON chmod +x rag_engine.py # Test python rag_engine.py stats ``` ### 4.4 Context Gatherer Installation ```bash cat > context_gatherer.py << 'EOFPYTHON' #!/usr/bin/env python3 """ Context Gatherer - Collects system context for Meta-Clawd """ import json import subprocess import os from pathlib import Path from datetime import datetime def run_command(cmd, timeout=5): """Run shell command safely""" try: result = subprocess.run( cmd, shell=True, capture_output=True, text=True, timeout=timeout ) return result.stdout.strip() except: return None def get_git_context(): """Git repository context""" # Check if in git repo if run_command("git rev-parse --git-dir 2>/dev/null") is None: return None return { 'repo': Path.cwd().name, 'branch': run_command("git branch --show-current"), 'status': run_command("git status --short"), 'recent_commits': run_command("git log --oneline -5"), 'diff_stats': run_command("git diff --stat"), 'uncommitted_changes': run_command("git diff --name-only") or "" } def get_file_context(): """Recently edited files""" # Files modified in last hour recent = run_command( "find . -type f -mmin -60 -name '*.py' -o -name '*.js' -o -name '*.md' 2>/dev/null | head -10" ) # Open files (via lsof) open_files = run_command( f"lsof -u {os.getenv('USER')} 2>/dev/null | grep -E '\\.(py|js|md|txt)$' | awk '{{print $NF}}' | head -10" ) return { 'cwd': str(Path.cwd()), 'recent_edits': recent.split('\n') if recent else [], 'open_files': open_files.split('\n') if open_files else [] } def get_shell_context(): """Shell history and environment""" history_file = Path.home() / ".bash_history" last_commands = [] if history_file.exists(): try: with open(history_file, 'r') as f: last_commands = f.readlines()[-20:] last_commands = [cmd.strip() for cmd in last_commands] except: pass return { 'last_commands': last_commands, 'shell': os.getenv('SHELL', '/bin/bash'), 'user': os.getenv('USER'), 'home': str(Path.home()) } def get_system_context(): """System resources""" load = run_command("uptime | awk -F'load average:' '{print $2}'") mem = run_command("free -h | awk 'NR==2{print $7}'") disk = run_command("df -h . | awk 'NR==2{print $4}'") return { 'load_average': load, 'memory_available': mem, 'disk_available': disk, 'hostname': run_command("hostname") } def gather_all(): """Gather complete context""" context = { 'timestamp': datetime.now().isoformat(), 'git': get_git_context(), 'files': get_file_context(), 'shell': get_shell_context(), 'system': get_system_context() } return context def main(): """CLI Interface""" context = gather_all() # Pretty print print(json.dumps(context, indent=2)) if __name__ == '__main__': main() EOFPYTHON chmod +x context_gatherer.py # Test python context_gatherer.py ``` ### 4.5 Session Manager Installation ```bash cat > session_manager.py << 'EOFPYTHON' #!/usr/bin/env python3 """ Session Manager - Handles branch sessions and comparison """ import json import subprocess import hashlib from pathlib import Path from datetime import datetime import sys import time class SessionManager: """Manages Clawd branch sessions""" def __init__(self, workspace=None): if workspace is None: workspace = Path.home() / "ai-workspace" / "sessions" self.workspace = Path(workspace) self.workspace.mkdir(parents=True, exist_ok=True) def create_branch(self, name, prompt, parent_session=None): """Create new branch session""" # Generate unique session ID session_id = hashlib.md5( f"{name}{datetime.now()}".encode() ).hexdigest()[:8] session_dir = self.workspace / f"{name}_{session_id}" session_dir.mkdir(exist_ok=True) # Metadata metadata = { 'id': session_id, 'name': name, 'created_at': datetime.now().isoformat(), 'parent': parent_session, 'status': 'created', 'prompt': prompt } (session_dir / 'metadata.json').write_text( json.dumps(metadata, indent=2) ) # Prompt file (session_dir / 'prompt.txt').write_text(prompt) print(f"✓ Created session: {session_id} ({name})") print(f" Directory: {session_dir}") return session_id, session_dir def start_session(self, session_id, command="clawd"): """Start session in tmux""" # Find session directory session_dir = self.find_session_dir(session_id) if not session_dir: print(f"✗ Session not found: {session_id}") return False # Read metadata metadata = json.loads((session_dir / 'metadata.json').read_text()) prompt = (session_dir / 'prompt.txt').read_text() # Tmux session name tmux_name = f"session-{session_id}" # Create tmux session subprocess.run([ 'tmux', 'new-session', '-d', '-s', tmux_name, '-c', str(session_dir) ]) # Send command subprocess.run([ 'tmux', 'send-keys', '-t', tmux_name, f'echo "Session: {metadata["name"]}"', 'Enter' ]) subprocess.run([ 'tmux', 'send-keys', '-t', tmux_name, f'echo "{prompt}" | tee initial_prompt.txt', 'Enter' ]) # Start logging log_file = session_dir / 'session.log' subprocess.run([ 'tmux', 'pipe-pane', '-t', tmux_name, f'cat >> {log_file}' ]) # Update metadata metadata['status'] = 'running' metadata['tmux_session'] = tmux_name metadata['started_at'] = datetime.now().isoformat() (session_dir / 'metadata.json').write_text( json.dumps(metadata, indent=2) ) print(f"✓ Started session {session_id}") print(f" Attach with: tmux attach -t {tmux_name}") print(f" Logs: {log_file}") return True def find_session_dir(self, session_id): """Find session directory by ID""" for path in self.workspace.glob(f"*_{session_id}"): if path.is_dir(): return path return None def get_session_status(self, session_id): """Get current session status""" session_dir = self.find_session_dir(session_id) if not session_dir: return None metadata = json.loads((session_dir / 'metadata.json').read_text()) # Check if tmux session is still running if 'tmux_session' in metadata: result = subprocess.run( ['tmux', 'has-session', '-t', metadata['tmux_session']], capture_output=True ) metadata['tmux_running'] = (result.returncode == 0) # Get log size log_file = session_dir / 'session.log' if log_file.exists(): metadata['log_size'] = log_file.stat().st_size return metadata def list_sessions(self): """List all sessions""" sessions = [] for session_dir in sorted(self.workspace.glob("*_*")): if (session_dir / 'metadata.json').exists(): metadata = json.loads( (session_dir / 'metadata.json').read_text() ) sessions.append(metadata) return sessions def compare_sessions(self, session_ids): """Compare multiple sessions""" comparisons = {} for sid in session_ids: session_dir = self.find_session_dir(sid) if not session_dir: continue metadata = json.loads( (session_dir / 'metadata.json').read_text() ) # Get output/log log_file = session_dir / 'session.log' output = log_file.read_text() if log_file.exists() else "" comparisons[sid] = { 'name': metadata['name'], 'created': metadata['created_at'], 'output_length': len(output), 'output_preview': output[:500] if output else "", 'metadata': metadata } return comparisons def main(): """CLI Interface""" if len(sys.argv) < 2: print(""" Usage: session_manager.py create <name> <prompt> session_manager.py start <session_id> session_manager.py status <session_id> session_manager.py list session_manager.py compare <id1> <id2> [id3...] Examples: session_manager.py create numpy-opt "Optimize using NumPy" session_manager.py start a3f9c2b4 session_manager.py compare a3f9c2b4 7d2e8f1a """) return mgr = SessionManager() command = sys.argv[1] if command == 'create': name = sys.argv[2] prompt = ' '.join(sys.argv[3:]) session_id, session_dir = mgr.create_branch(name, prompt) print(f"\nNext: session_manager.py start {session_id}") elif command == 'start': session_id = sys.argv[2] mgr.start_session(session_id) elif command == 'status': session_id = sys.argv[2] status = mgr.get_session_status(session_id) if status: print(json.dumps(status, indent=2)) else: print(f"Session not found: {session_id}") elif command == 'list': sessions = mgr.list_sessions() print(f"\n📋 Sessions ({len(sessions)}):\n") for s in sessions: status_emoji = "🟢" if s.get('status') == 'running' else "⚪" print(f"{status_emoji} {s['id']} - {s['name']}") print(f" Created: {s['created_at']}") print() elif command == 'compare': session_ids = sys.argv[2:] comparisons = mgr.compare_sessions(session_ids) print("\n🔍 Session Comparison:\n") for sid, data in comparisons.items(): print(f"Session: {sid} ({data['name']})") print(f" Output length: {data['output_length']} chars") print(f" Preview: {data['output_preview'][:100]}...") print() else: print(f"Unknown command: {command}") if __name__ == '__main__': main() EOFPYTHON chmod +x session_manager.py ``` ### 4.6 System Prompt für Meta-Clawd ```bash cat > system_prompt.md << 'EOF' # META-CLAWD ORCHESTRATOR - System Prompt You are Meta-Clawd, an AI orchestrator that manages context, coordinates agents, and enhances prompts. ## Your Capabilities ### 1. Context Management - Gather system context (git, files, shell history) - Search knowledge base using RAG - Index new information automatically - Track conversation history ### 2. Session Branching - Create parallel experiment sessions when uncertain - Compare results objectively - Merge best solutions - Learn from all branches ### 3. Agent Coordination - Identify when to delegate tasks - Spawn specialized agents (coding, research, debug, optimize) - Monitor progress - Synthesize results ### 4. Prompt Enhancement - Analyze user intent deeply - Add relevant context from knowledge base - Optimize prompt structure for target agents - Warn about potential issues ## Available Tools You have access to these tools via shell commands: ```bash # RAG Operations python tools/rag_engine.py search "<query>" [--type TYPE] [--limit N] python tools/rag_engine.py index <file> python tools/rag_engine.py index-dir <directory> python tools/rag_engine.py stats # Context Gathering python tools/context_gatherer.py # Returns JSON with git, files, shell, system context # Session Management python tools/session_manager.py create <name> "<prompt>" python tools/session_manager.py start <session_id> python tools/session_manager.py status <session_id> python tools/session_manager.py list python tools/session_manager.py compare <id1> <id2> ``` ## Workflow For each user request: 1. **Understand Intent** - What does the user REALLY want? - Are they asking the right question? 2. **Gather Context** - Run context_gatherer.py for system state - Search RAG for relevant past work - Check git status if relevant 3. **Decide Strategy** - Can I solve this directly? → Do it - Am I uncertain? → Create branch sessions to explore - Is this complex? → Delegate to specialized agent - Do I need more info? → Ask clarifying questions 4. **Execute** - Use appropriate tools - Monitor progress - Collect results 5. **Learn & Update** - Index important conversations - Note successful patterns - Update knowledge base ## Personality - **Proactive:** Don't just answer, improve the question - **Transparent:** Explain your reasoning - **Thorough:** Use tools aggressively, don't guess - **Collaborative:** Work WITH the user, not FOR them - **Learning:** Every interaction improves future ones ## Examples **User:** "Fix my code" **Bad Response:** "Please share your code." **Good Response:** Let me gather context first... ```bash python tools/context_gatherer.py ``` I see you're in `my-project/` working on `utils.py`. Recent git changes show modifications to `calculate_metrics()`. Let me search for related issues: ```bash python tools/rag_engine.py search "bug calculate_metrics" --type code ``` Found 3 similar issues you've solved before. The pattern suggests this might be a performance issue, not a bug. Should I: A) Analyze the current code and suggest fixes B) Create parallel sessions to test different optimization approaches C) Search for the specific error message first --- **User:** "Optimize this function" **Your Response:** I'll create two parallel approaches: ```bash python tools/session_manager.py create numpy-approach "Optimize calculate_metrics() using NumPy vectorization. Context: [current code]" python tools/session_manager.py create cython-approach "Optimize calculate_metrics() using Cython. Context: [current code]" ``` Both sessions running. I'll compare results in ~5 minutes. Meanwhile, I'm indexing this conversation: ```bash echo "[conversation]" | python tools/rag_engine.py index conversation ``` --- ## Remember - Always use tools, don't rely on memory alone - When in doubt, branch and experiment - Index valuable knowledge for future use - Be honest about uncertainty - Focus on user's actual goal, not literal request You are not just an assistant - you are a meta-cognitive layer that makes the entire AI system smarter. EOF ``` ### 4.7 Tools Configuration ```bash cat > tools_config.yaml << 'EOF' tools: rag_search: command: "cd {workspace}/tools && python rag_engine.py search '{query}' --limit {limit}" description: "Search knowledge base semantically" parameters: - query: string (required) - limit: integer (default: 5) rag_index: command: "cd {workspace}/tools && python rag_engine.py index '{path}'" description: "Index a file into knowledge base" parameters: - path: string (required) rag_index_dir: command: "cd {workspace}/tools && python rag_engine.py index-dir '{directory}'" description: "Index entire directory" parameters: - directory: string (required) context_gather: command: "cd {workspace}/tools && python context_gatherer.py" description: "Gather current system context (git, files, shell)" parameters: [] session_create: command: "cd {workspace}/tools && python session_manager.py create '{name}' '{prompt}'" description: "Create new branch session" parameters: - name: string (required) - prompt: string (required) session_start: command: "cd {workspace}/tools && python session_manager.py start {session_id}" description: "Start a created session" parameters: - session_id: string (required) session_compare: command: "cd {workspace}/tools && python session_manager.py compare {session_ids}" description: "Compare multiple sessions" parameters: - session_ids: string (space-separated IDs) variables: workspace: "~/ai-workspace/meta-orchestrator" EOF ``` --- ## 5. KONFIGURATION ### 5.1 Environment Setup ```bash # .envrc (optional, wenn du direnv nutzt) cat > ~/.config/meta-clawd/env << 'EOF' export META_CLAWD_WORKSPACE=~/ai-workspace/meta-orchestrator export META_CLAWD_AUTO_INDEX=true export META_CLAWD_MIN_SIMILARITY=0.6 EOF # Oder in ~/.bashrc / ~/.zshrc cat >> ~/.bashrc << 'EOF' # Meta-Clawd Shortcuts alias meta-clawd='cd ~/ai-workspace/meta-orchestrator && clawd --system-prompt system_prompt.md' alias rag-search='python ~/ai-workspace/meta-orchestrator/tools/rag_engine.py search' alias rag-index='python ~/ai-workspace/meta-orchestrator/tools/rag_engine.py index' alias ctx='python ~/ai-workspace/meta-orchestrator/tools/context_gatherer.py' EOF source ~/.bashrc ``` ### 5.2 Initial Index Population ```bash cd ~/ai-workspace/meta-orchestrator # Indexiere deine bestehenden Projekte python tools/rag_engine.py index-dir ~/projects --recursive # Indexiere Dokumentation (falls vorhanden) python tools/rag_engine.py index-dir ~/Documents/notes # Stats anschauen python tools/rag_engine.py stats ``` --- ## 6. ERSTE NUTZUNG ### 6.1 Meta-Clawd starten ```bash cd ~/ai-workspace/meta-orchestrator # Starte Meta-Clawd mit System Prompt clawd --system-prompt system_prompt.md # Oder mit Alias meta-clawd ``` ### 6.2 Erster Test ``` [You] Hey Meta-Clawd! Teste deine Tools: 1. Gather system context 2. Search knowledge base for "optimization" 3. Show me stats [Meta-Clawd sollte antworten und Tools nutzen...] ``` ### 6.3 Beispiel-Session: Code-Optimierung ``` [You] Ich habe eine langsame Funktion in ~/projects/my-app/utils.py:process_data() Die läuft auf 10K Zeilen und dauert 5 Sekunden. Zu langsam! [Meta-Clawd] Let me gather context and search for similar optimization work... [Führt aus:] 1. python tools/context_gatherer.py 2. python tools/rag_engine.py search "slow function optimization" --type code 3. cat ~/projects/my-app/utils.py | grep -A 50 "def process_data" [Analysiert und schlägt vor:] I found 3 previous optimizations with similar patterns. Most effective was switching from pandas to NumPy for large datasets. Should I: A) Create 2 branch sessions (NumPy vs Polars) and compare results B) Profile the function first to find exact bottleneck C) Show you the previous optimization code for reference [You] A - create branches [Meta-Clawd] Creating parallel optimization attempts... [Führt aus:] python tools/session_manager.py create numpy-opt "Optimize process_data() using NumPy. Current bottleneck: DataFrame operations. Context: [function code]" python tools/session_manager.py create polars-opt "Optimize process_data() using Polars. Current bottleneck: DataFrame operations. Context: [function code]" ✓ Created 2 sessions ✓ Both running in tmux You can monitor with: tmux attach -t session-<id> I'll compare results when both finish. Estimated time: 5-10 minutes. ``` --- ## 7. ERWEITERTE FEATURES ### 7.1 Auto-Indexing Watcher ```bash cat > ~/ai-workspace/meta-orchestrator/tools/auto_indexer.py << 'EOFPYTHON' #!/usr/bin/env python3 """Auto-index changed files""" from watchdog.observers import Observer from watchdog.events import FileSystemEventHandler from pathlib import Path import time import sys # Import RAG engine sys.path.insert(0, str(Path(__file__).parent)) from rag_engine import RAGEngine class AutoIndexer(FileSystemEventHandler): def __init__(self, watch_dirs, extensions): self.rag = RAGEngine() self.extensions = set(extensions) print(f"Auto-indexer watching: {watch_dirs}") print(f"Extensions: {extensions}") def on_modified(self, event): if event.is_directory: return path = Path(event.src_path) if path.suffix in self.extensions: print(f"Indexing: {path}") try: self.rag.index_code_file(path) self.rag.save_index() except Exception as e: print(f"Error indexing {path}: {e}") def main(): watch_dirs = [ Path.home() / "projects", Path.home() / "ai-workspace" ] extensions = ['.py', '.js', '.ts', '.md', '.txt'] indexer = AutoIndexer(watch_dirs, extensions) observer = Observer() for directory in watch_dirs: if directory.exists(): observer.schedule(indexer, str(directory), recursive=True) print(f"✓ Watching {directory}") observer.start() print("\n🔍 Auto-indexer running. Press Ctrl+C to stop.") try: while True: time.sleep(1) except KeyboardInterrupt: observer.stop() observer.join() if __name__ == '__main__': # Install: pip install watchdog try: from watchdog.observers import Observer from watchdog.events import FileSystemEventHandler main() except ImportError: print("Error: watchdog not installed. Run: pip install watchdog") EOFPYTHON chmod +x tools/auto_indexer.py # Als Background-Service laufen lassen # In systemd oder einfach: nohup python tools/auto_indexer.py > auto_indexer.log 2>&1 & ``` ### 7.2 Web Dashboard (Optional) ```bash cat > ~/ai-workspace/meta-orchestrator/tools/dashboard.py << 'EOFPYTHON' #!/usr/bin/env python3 """Simple web dashboard for Meta-Clawd""" from flask import Flask, render_template_string, jsonify import sys from pathlib import Path sys.path.insert(0, str(Path(__file__).parent)) from rag_engine import RAGEngine from session_manager import SessionManager app = Flask(__name__) rag = RAGEngine() sessions = SessionManager() TEMPLATE = """ <!DOCTYPE html> <html> <head> <title>Meta-Clawd Dashboard</title> <style> body { font-family: system-ui; margin: 20px; background: #1a1a2e; color: #eee; } .card { background: #16213e; padding: 20px; margin: 20px 0; border-radius: 8px; } h1 { color: #00d9ff; } .stat { display: inline-block; margin: 10px 20px; } table { width: 100%; border-collapse: collapse; } th, td { padding: 10px; text-align: left; border-bottom: 1px solid #0f3460; } th { color: #00d9ff; } </style> </head> <body> <h1>🎯 Meta-Clawd Dashboard</h1> <div class="card"> <h2>📊 Knowledge Base</h2> <div class="stat">Total Documents: <strong id="total-docs">-</strong></div> <div class="stat">Code Files: <strong id="code-docs">-</strong></div> <div class="stat">Conversations: <strong id="conv-docs">-</strong></div> </div> <div class="card"> <h2>🔄 Active Sessions</h2> <table id="sessions-table"> <tr><th>ID</th><th>Name</th><th>Status</th><th>Created</th></tr> </table> </div> <script> async function loadStats() { const stats = await fetch('/api/stats').then(r => r.json()); document.getElementById('total-docs').textContent = stats.total_documents; document.getElementById('code-docs').textContent = stats.by_type.code || 0; document.getElementById('conv-docs').textContent = stats.by_type.conversation || 0; } async function loadSessions() { const sessions = await fetch('/api/sessions').then(r => r.json()); const table = document.getElementById('sessions-table'); sessions.forEach(s => { const row = table.insertRow(); row.innerHTML = `<td>${s.id}</td><td>${s.name}</td><td>${s.status}</td><td>${s.created_at}</td>`; }); } loadStats(); loadSessions(); setInterval(loadStats, 10000); // Refresh every 10s </script> </body> </html> """ @app.route('/') def index(): return render_template_string(TEMPLATE) @app.route('/api/stats') def api_stats(): return jsonify(rag.stats()) @app.route('/api/sessions') def api_sessions(): return jsonify(sessions.list_sessions()) if __name__ == '__main__': print("🌐 Dashboard running at http://localhost:5555") app.run(host='0.0.0.0', port=5555) EOFPYTHON chmod +x tools/dashboard.py # Start dashboard python tools/dashboard.py # → Open http://localhost:5555 ``` --- ## 8. TROUBLESHOOTING ### 8.1 Häufige Probleme **Problem:** RAG Engine findet keine Ergebnisse ```bash # Check index python tools/rag_engine.py stats # Wenn leer: Re-index python tools/rag_engine.py index-dir ~/projects ``` **Problem:** tmux Session startet nicht ```bash # Check tmux tmux list-sessions # Falls Fehler: Installiere tmux sudo apt install tmux # Debian/Ubuntu sudo pacman -S tmux # Arch brew install tmux # macOS ``` **Problem:** Context Gatherer findet nichts ```bash # Check git cd ~/projects/your-project git status # Check lsof lsof -u $USER | head ``` **Problem:** Embedding Model lädt nicht ```bash # Check Internet connection ping huggingface.co # Manuell herunterladen python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('all-MiniLM-L6-v2')" ``` ### 8.2 Logs & Debugging ```bash # Enable verbose logging export META_CLAWD_DEBUG=1 # Check RAG database sqlite3 ~/ai-workspace/meta-orchestrator/knowledge.db "SELECT COUNT(*) FROM documents;" # Check FAISS index python -c "import faiss; idx = faiss.read_index('faiss.index'); print(f'Vectors: {idx.ntotal}')" # Session logs cat ~/ai-workspace/sessions/*/session.log ``` --- ## 9. MAINTENANCE & UPDATES ### 9.1 Regular Maintenance ```bash # Weekly: Cleanup old sessions (>30 days) find ~/ai-workspace/sessions -type d -mtime +30 -exec rm -rf {} \; # Monthly: Optimize database sqlite3 ~/ai-workspace/meta-orchestrator/knowledge.db "VACUUM;" # Monthly: Rebuild index (if corrupted) cd ~/ai-workspace/meta-orchestrator mv faiss.index faiss.index.backup python tools/rag_engine.py index-dir ~/projects ``` ### 9.2 Backups ```bash # Backup script cat > ~/ai-workspace/backup.sh << 'EOF' #!/bin/bash BACKUP_DIR=~/backups/meta-clawd DATE=$(date +%Y%m%d) mkdir -p $BACKUP_DIR # Backup knowledge base tar -czf $BACKUP_DIR/knowledge-$DATE.tar.gz \ ~/ai-workspace/meta-orchestrator/knowledge.db \ ~/ai-workspace/meta-orchestrator/faiss.index* # Keep only last 7 backups ls -t $BACKUP_DIR/knowledge-*.tar.gz | tail -n +8 | xargs rm -f echo "✓ Backup complete: $BACKUP_DIR/knowledge-$DATE.tar.gz" EOF chmod +x ~/ai-workspace/backup.sh # Add to cron (weekly) crontab -e # Add line: # 0 2 * * 0 ~/ai-workspace/backup.sh ``` ### 9.3 Updates ```bash # Update dependencies cd ~/ai-workspace/meta-orchestrator source venv/bin/activate pip install --upgrade sentence-transformers faiss-cpu transformers torch # Pull latest tools (if you have them in git) git pull origin main ``` --- ## 10. USAGE PATTERNS & BEST PRACTICES ### 10.1 When to Use What | Situation | Tool | Why | |-----------|------|-----| | "How did I solve X before?" | `rag_search` | Finds past solutions | | "What am I working on?" | `context_gather` | Current state | | "Try 2 approaches" | `session_create` (2x) | Parallel experiments | | "This is complex" | Delegate to agent | Specialized handling | | "Need to remember this" | Index conversation | Future reference | ### 10.2 Workflow Examples **Research Task:** ``` 1. rag_search for existing knowledge 2. If gaps: create research-agent session 3. Agent gathers info 4. Index results 5. Synthesize answer ``` **Bug Fixing:** ``` 1. context_gather (what changed recently?) 2. rag_search for similar bugs 3. If found: apply previous fix 4. If new: create debug-agent session 5. Index solution ``` **Optimization:** ``` 1. Profile current code 2. rag_search for optimization patterns 3. Create 2-3 branch sessions (different approaches) 4. Compare results 5. Merge best 6. Index winning approach ``` --- ## 11. KOSTEN & RESSOURCEN ### 11.1 Disk Usage ``` Component Size ───────────────────────────── RAG Engine binary ~80MB FAISS Index ~1MB per 1000 docs SQLite DB ~500KB per 1000 docs Session logs ~10MB per session Total (typical) ~500MB - 2GB ``` ### 11.2 RAM Usage ``` Component RAM ───────────────────────────── Sentence Transformers ~500MB FAISS Index (loaded) ~Size of index file Python overhead ~200MB Total ~1GB ``` ### 11.3 CPU Usage ``` Operation CPU Time ───────────────────────────────── Embedding 1 doc ~10-50ms Search 10K docs ~20-100ms Index 1000 files ~30-60s Context gathering ~100-500ms ``` ### 11.4 Financial Costs ``` Item Cost ────────────────────────────── All software $0 (open source) Electricity (idle) ~$0.01/day Electricity (active) ~$0.05/day API calls (Clawd) Variable (depends on provider) Total recurring ~$1-3/month ``` --- ## 12. NEXT STEPS ### 12.1 Getting Started Checklist - [ ] Workspace created - [ ] Python environment setup - [ ] RAG Engine installed & tested - [ ] Context Gatherer working - [ ] Session Manager working - [ ] System prompt configured - [ ] Initial index populated - [ ] First Meta-Clawd session successful ### 12.2 First Week Goals - [ ] Index all your existing projects - [ ] Use RAG search 10+ times - [ ] Create at least 2 branch sessions - [ ] Compare session results - [ ] Index 3+ conversations - [ ] Customize system prompt to your style ### 12.3 Advanced Goals - [ ] Setup auto-indexer - [ ] Create custom specialized agent - [ ] Integrate with your CI/CD - [ ] Build personal knowledge graph - [ ] Fine-tune embedding model on your data - [ ] Multi-modal indexing (images, PDFs) --- ## 13. ARCHITECTURE DECISIONS ### 13.1 Why These Technologies? **sentence-transformers:** - Lightweight (~80MB) - Fast (CPU-friendly) - Good quality embeddings - No API needed **FAISS:** - Extremely fast similarity search - Scales to millions of vectors - Low memory footprint - Battle-tested (Meta/Facebook) **SQLite:** - Zero config - Single file - ACID compliant - Fast for metadata **tmux:** - Ubiquitous - Persistent sessions - Easy monitoring - Scriptable **Python:** - Ecosystem for ML/AI - Easy to extend - Cross-platform - Readable ### 13.2 Alternative Approaches | Component | Current | Alternative | Tradeoff | |-----------|---------|-------------|----------| | Embeddings | sentence-transformers | OpenAI API | Cost vs Quality | | Vector DB | FAISS | ChromaDB, Pinecone | Simplicity vs Features | | Sessions | tmux | Docker, k8s | Complexity vs Scalability | | Storage | SQLite | PostgreSQL | Setup vs Power | | Orchestrator | Clawd | LangChain | Custom vs Framework | --- ## ZUSAMMENFASSUNG Du hast jetzt: ✅ **Komplettes Meta-Clawd System** - RAG Engine für semantische Suche - Session Manager für parallele Experimente - Context Gatherer für System-State - Alles orchestriert durch Nano-Clawd ✅ **Installation in ~2-4 Stunden** - Copy-paste friendly - Schritt-für-Schritt getestet - Minimal dependencies ✅ **$0 laufende Kosten** - Alles lokal - Open Source - Kein API-Lock-in ✅ **Sofort produktiv** - Use cases klar - Workflows dokumentiert - Beispiele gegeben ✅ **Erweiterbar** - Modular design - Easy to customize - Community-friendly **Nächster Schritt:** Fang einfach an! Kopiere die Commands, führe sie aus, und in 2-3 Stunden hast du ein laufendes System. **Fragen? Issues? Improvements?** → Das System ist so designed dass es MIT dir lernt und wächst. Viel Erfolg! 🚀 --- **Dokument-Version:** 1.0 **Letzte Änderung:** 2025-01-15 **Maintainer:** [Your Name] **License:** MIT / CC-BY-4.0 **Repository:** [Optional: GitHub Link]


==================================================

Bitte beachte den obigen Kontext für deine Antwort.

==================================================

Bitte beachte den obigen Kontext für deine Antwort.